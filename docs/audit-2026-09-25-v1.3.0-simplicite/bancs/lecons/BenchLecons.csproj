<Project Sdk="Microsoft.NET.Sdk">
  <!-- Banc hors application, zone lecons (audit 2026-09-25). Réplique d'algorithmes de
       LearningModule, LessonCatalog, LessonHintProvider et LessonsWindow @ f0a98ba.
       Ne référence pas AZERTY Global.dll. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <Optimize>true</Optimize>
  </PropertyGroup>
</Project>
