# Audit v1.3.0 — axe « coquille applicative »

Dépôt : `D:\My files\Keyboard Layouts\projects\azerty-global\components\microsoft-store`
Branche `release/1.2.0-notation-store`, HEAD `4d5da24`. Lecture seule, aucun binaire exécuté.
Chemins relatifs ci-dessous = relatifs à la racine du dépôt ci-dessus.
Constats uniquement : ce que fait le code, et ce qu'il fait sur telle entrée. Aucune gravité, aucun verdict global.

---

## 1. Démarrage

Séquence de `Main` : DPI-aware → lecture de la langue (donc de `config.json` **et** du registre de stratégie) → mutex d'instance unique → journal `SecondInstance` si doublon → rotation d'`error.log` → pose du handler d'exception fatale → `new TrayApplication()` → `Run()`.

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 1.1 | `src/Program.cs:30` | `var mutexName = $"Local\\{ProductIdentity.SingleInstanceMutexName}.{sid}";` | Nom effectif `Local\AZERTYGlobalSingleInstance.<SID utilisateur>`. `Local\` = espace de noms **de session**, pas de machine : deux sessions Windows du **même** compte (console + RDP, changement rapide d'utilisateur) obtiennent chacune `isNew = true` et lancent deux instances, qui partagent le même `%LocalAppData%` (donc `config.json`, `usage-stats.json`, `error.log`). Le commentaire ligne 27-28 assume ce choix (« Local\ scope = current session uniquement, donc safe ») au regard du squat de nom, pas du partage de fichiers. | aucun trouvé |
| 1.2 | `src/Program.cs:12-13` | `BuildSecondInstanceLogDetails(bool packaged, string[] args) =>`<br>`$"packaged={packaged}, argCount={args.Length}";` | Seules la forme d'activation et le nombre d'arguments sont journalisés ; jamais les arguments bruts (URI, chemin, jeton). | `QuickWinsTests.SecondInstanceLog_DoesNotContainRawArguments` (`src/AZERTYGlobal.Tests/QuickWinsTests.cs:57`) |
| 1.3 | `src/Program.cs:38` puis `:60` | `ConfigManager.LogCompatEvent("SecondInstance",` … puis `ConfigManager.RotateLogIfNeeded();` | La seconde instance **écrit** dans `error.log` (ligne 38) puis `return` ligne 56 : elle ne passe jamais par la rotation ligne 60. Un poste où seules des secondes instances se lancent (clic de toast sans activateur COM) fait grossir `error.log` sans jamais le faire tourner. | aucun trouvé |
| 1.4 | `src/Program.cs:63-77` | `AppDomain.CurrentDomain.UnhandledException += (_, e) =>` … `File.AppendAllText(logPath, $"[{DateTime.Now:s}] FATAL: {safeMessage}\n");` | Exception non gérée : ligne `FATAL:` assainie dans `error.log`, `MessageBox` d'erreur, puis terminaison (le handler ne l'empêche pas). Exception **pendant le ctor de TrayApplication** : `src/TrayApplication.cs:313-323`, journalisée + `MessageBoxW(L.Tray_StartupError)` + `PostQuitMessage(1)`, l'app sort de `Run()` immédiatement et `using var app` appelle `Dispose → Cleanup`. | aucun trouvé |
| 1.5 | `src/ConfigManager.cs:691-693` | `public static string LogDirectory => _logDirectoryOverride ?? (IsPackaged`<br>`? Path.Combine(Environment.GetFolderPath(…LocalApplicationData), ProductIdentity.ConfigFolderName)` | Chemin demandé : `%LocalAppData%\AZERTY Global\error.log` en packagé, `AppContext.BaseDirectory\error.log` hors package. Sous MSIX les écritures sont redirigées : `…\Packages\AZERTYGlobal.AZERTYGlobal_w9kghr08zmhbg\LocalCache\Local\AZERTY Global\` — mesuré le 2026-09-19, `docs/audit-2026-09-15-v1.2.0/recette-vm-resultats.md:51`. | aucun trouvé (test unitaire) ; recette VM-01 |
| 1.6 | `src/ConfigManager.cs:719-720` | `msg = System.Text.RegularExpressions.Regex.Replace(msg,`<br>`@"[A-Z]:\\Users\\[^\\]+\\", @"<UserDir>\");` | Assainissement : type d'exception + **première ligne** du message, tronquée à 200 caractères, chemins de profil masqués. Non couverts par la regex : chemin hors profil (`D:\Jeux\x.exe`), chemin en barres obliques (`C:/Users/x/`), chemin UNC. Aucune pile d'appel (`StackTraceSupport=false` dans le csproj ligne 28). | aucun trouvé |
| 1.7 | `src/ConfigManager.cs:758-765` | `public static string AnonymizeProcessName(string? procName)` … `return "hash:" + Convert.ToHexString(hashBytes, 0, 4).ToLowerInvariant();` | Les noms de processus au premier plan ne sont jamais écrits en clair dans `error.log` : HMAC-SHA256 tronqué à 4 octets, sel aléatoire de 16 octets persisté en clair dans `config.json` sous `_compat_log_salt` (`:771-777`). Tous les sites d'appel passent par lui (`TrayApplication.cs:434, 1507, 1514, 2261, 2266, 2271` ; `TypingEngine.Windows/ForegroundMonitor.cs:234`). | aucun trouvé |
| 1.8 | `src/ConfigManager.cs:805, 812-827` | `private const long LogRotationSizeBytes = 5 * 1024 * 1024; // 5 Mo` … `File.Move(logFile, oldFile);` | Rotation **une seule fois, au démarrage** : une session longue peut dépasser 5 Mo sans rotation. Une seule génération conservée, `error.log.old` précédent supprimé. Taille maximale sur disque en régime : ~10 Mo. | aucun trouvé |
| 1.9 | `src/ConfigManager.cs:742` | `sans contenu (process name, mode, action) — pas de codepoint, pas de char.` | Aucun caractère tapé, aucun titre de fenêtre, aucun chemin utilisateur n'est écrit dans `error.log` par conception. Le seul identifiant technique en clair est le **PID** du processus au premier plan, et uniquement si `compatibilityDebugLog` est vrai (`TypingEngine.Windows/ForegroundMonitor.cs:235`, `pid={pid}`). | `ConfigManagerCompatTests.LogCompatEvent_ConcurrentWrites_ProducesCompleteLines` (`:146`) |

**Correction minimale possible** — 1.1 : préfixer `Global\` au lieu de `Local\` (le SID rend déjà le nom inattaquable par un autre compte). 1.3 : appeler `RotateLogIfNeeded()` avant le `LogCompatEvent("SecondInstance", …)`. 1.6 : ajouter `[A-Z]:[\\/](?:Users|Utilisateurs)[\\/][^\\/]+[\\/]` et un masquage générique des chemins absolus. 1.8 : rappeler `RotateLogIfNeeded()` depuis le tick `TIMER_STATS_FLUSH`.

---

## 2. Configuration

Chemin : `%LocalAppData%\AZERTY Global\config.json` en packagé (redirigé MSIX, cf. 1.5), `<dossier de l'exe>\config.json` hors package. Format : objet JSON plat, indenté, UTF-8 sans BOM, plus un unique sous-objet `compatibility`.

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 2.1 | `src/ConfigManager.cs:980, 1013-1016` | `using (var stream = new FileStream(tempPath, FileMode.Create, FileAccess.Write, FileShare.None))` … `File.Replace(tempPath, _configPath, null, true);` | Écriture **atomique** : fichier temporaire `config.json.tmp`, `stream.Flush(true)` (flush disque), puis `File.Replace` si la cible existe, `File.Move` sinon. Même motif dans `UsageStats.SaveLocked` (`:528, 551-554`) et `LessonProgressStore.Save` (`:256, 286-289`). | `UsageStatsTests.Flush_PersistsDirtyState_SurvivesReload` (`:219`) ; aucun test ne lit le `.tmp` |
| 2.2 | `src/ConfigManager.cs:942-946` | `catch (Exception ex) when (ex is IOException or JsonException or UnauthorizedAccessException)`<br>`{ _loadFailed = true; Log("ConfigManager.EnsureLoaded", ex); }` | JSON invalide ou tronqué : le cache reste vide, la session **tourne entièrement sur les défauts** (langue « fr », notifications activées, autostart cache à faux…). Sur `"{ invalid"` : `ShowOnboardingAtStartup` vaut vrai, `ReviewPromptCount` 0, `appLanguage` « fr ». | `ConfigManagerCompatTests.Save_DoesNotOverwriteExistingConfig_WhenLoadFailed` (`:188`) |
| 2.3 | `src/ConfigManager.cs:970-974` | `if (_loadFailed && File.Exists(_configPath))` … `Log("ConfigManager.Save", new IOException("Sauvegarde ignoree: config.json existant non charge."));` | Le fichier illisible n'est **jamais écrasé** : aucune perte de données utilisateur sur disque. Contrepartie mesurable : toute modification faite pendant cette session (langue, notifications, bornes de fenêtre, override de compatibilité) est perdue à la fermeture, et l'utilisateur n'en est averti que par une ligne d'`error.log`. | `ConfigManagerCompatTests.Save_DoesNotOverwriteExistingConfig_WhenLoadFailed` (`:188`) |
| 2.4 | `src/ConfigManager.cs:938` | `_cache[prop.Name] = prop.Value.Clone();` | Toutes les clés inconnues de premier niveau sont **conservées** en mémoire et réécrites. Aller-retour testé sur un `config.json` de v0.9.7. | `ConfigManagerCompatTests.Downgrade_V0_9_6_Tolerates_CompatibilityBlock` (`:85`) |
| 2.5 | `src/ConfigManager.cs:991, 993` | `case JsonValueKind.Number: writer.WriteNumberValue(val.GetDouble()); break;`<br>`default: writer.WriteStringValue(val.ToString()); break;` | Deux transformations silencieuses à la réécriture : (a) tout nombre repasse par un `double` — un entier > 2^53 posé par une version future perd de la précision ; (b) toute valeur **non scalaire** (objet ou tableau) autre que `compatibility`, ainsi que `null`, est réécrite **en chaîne** — sur l'entrée `{"x": {"a":1}}` la sortie est `"x": "{\"a\":1}"`, sur `{"x": null}` la sortie est `"x": ""`. Une clé écrite par une version plus récente ne survit donc pas intacte à un passage par la v1.3.0. | aucun trouvé |
| 2.6 | `src/ConfigManager.cs:335-358` | `/// Migration v1.1 → v1.3 (décision d'Antoine du 2026-09-20) : le booléen hérité`<br>`/// `reviewPromptDone` ne vaut plus une sollicitation consommée.` | Migration depuis la 1.1.0 : `ReviewPromptCount => (int)GetUInt("reviewPromptCount")` — **aucune** lecture de `reviewPromptDone`. Sur `{"reviewPromptDone": true}` : 0 essai consommé, deux essais entiers. Sur `{"reviewPromptDone": true, "reviewPromptCount": 2}` : 2. `reviewPromptClicked` (né en v1.2.0) reste lu et bloquant. Aucune autre clé n'est migrée ou renommée. | `ReviewPromptConfigTests.ReviewPromptCount_LegacyDoneFlagNoLongerConsumesAnAttempt` (`:141`), `…_ResetDoesNotRevivePromptsForAClickedInstall` (`:163`), `…_ExplicitCountWinsOverLegacyFlag` (`:174`) |
| 2.7 | `src/ConfigManager.cs:400` | `SetBool("reviewPromptDone", true);` | `reviewPromptDone` continue d'être **écrit** à chaque sollicitation alors que plus aucun code ne le lit : clé morte du point de vue de la 1.3.0, conservée pour un retour arrière en v1.1 (commentaire `:380-381`). | `ReviewPromptConfigTests.RecordReviewPromptShown_IncrementsAndPersists` (`:84`) |
| 2.8 | `src/ConfigManager.cs:130-153` | `internal static bool ShowOnboardingUserSetting` … `_cache["showOnboardingAtStartup"] = doc.RootElement.Clone();`<br>`Save();` | Un **getter écrit sur disque** : la première lecture d'un `config.json` sans la clé déclenche un `Save()` complet. Même motif en `:954-962` (installation neuve : création du fichier pour y poser `appLanguage`) et en `:768-778` (création du sel de journal). | `LocalizationTests.InstallationNeuve_EcritLaLangueDeriveDeWindows` (`:31`), `…ConfigExistanteSansCle_ResteEnFrancais` (`:47`) |
| 2.9 | `src/ConfigManager.cs:967` + 1.1 | `string tempPath = _configPath + ".tmp";` | Écritures concurrentes : le chemin temporaire est **fixe et partagé**. Deux instances du même utilisateur (cf. 1.1) → le second `FileStream(…, FileShare.None)` lève `IOException`, capturée `:1018` et seulement journalisée ; et chaque processus tient son propre cache, donc le dernier `Save()` complet écrase les clés écrites par l'autre. Sur un dossier synchronisé (mode hors package, exe dans un dossier OneDrive/Proton), `File.Replace` peut échouer de la même façon — l'échec est silencieux côté interface. | aucun trouvé |
| 2.10 | `src/ConfigManager.cs:561-575` | `_compatibilityCache[processName] = mode;` | `config.json` contient en clair les **noms de processus** pour lesquels l'utilisateur a posé un override (sous-objet `compatibility`). C'est le seul endroit du produit où un nom de processus est persisté non anonymisé. | `ConfigManagerCompatTests.SetCompatibilityOverride_ForceOn_PersistsAndReadsBack` (`:35`) |

**Correction minimale possible** — 2.5 : remplacer le `default:` par `val.WriteTo(writer)` et le cas `Number` par `writer.WriteRawValue(val.GetRawText())`. 2.9 : suffixer le `.tmp` du PID (`_configPath + "." + Environment.ProcessId + ".tmp"`). 2.3 : remonter l'échec de sauvegarde à l'utilisateur une fois par session.

---

## 3. Réseau et sorties

**Aucun** `HttpClient`, `WebRequest`, `WebClient`, `Socket` ni `Dns` dans tout `src/` (hors `bin`/`obj`) — grep vérifié. Aucun `Process.Start`. Toutes les sorties passent par `Win32.ShellExecuteW(…, "open", …)` ou par l'API Store WinRT.

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 3.1 | `src/ProductIdentity.cs:46-49, 54-55` | `public const string SiteBaseUrl = "https://" + SiteDomain;`<br>`"ms-windows-store://review/?ProductId=" + StoreProductId;` | URL littérales : `https://azerty.global`, `https://discord.gg/nYknqshJz3`, `https://github.com/AMCF-asso/azerty-global-app`, `ms-windows-store://review/?ProductId=9N4BTS43SSSZ`. | `AppChannelTests.StorePackageFamilyName_MatchesTheInstalledPackage` (`:23`) pour l'identité, pas pour les URL |
| 3.2 | `src/TrayApplication.cs:681-692` | `case IDM_GUIDE_CHANGES: Win32.ShellExecuteW(IntPtr.Zero, "open", ProductIdentity.Url("/guide"), null, null, 1); break;` | Menu de la zone de notification, 8 ouvertures : `/guide`, `/nouveautes`, `/assets/Prise_en_main_AZERTY_Global.pdf`, `/guide#cartes`, `/mentions-legales#confidentialite-securite`, Discord, racine du site, `/feedback`, `/soutien`. Toutes construites par concaténation de constantes, aucune donnée utilisateur. | `SoberChannelTests.*` (`:25-87`) couvrent la **présence** des entrées, pas les URL |
| 3.3 | `src/TrayApplication.cs:2036` | `Win32.ShellExecuteW(IntPtr.Zero, "open", ProductIdentity.Url("/feedback?source=app-notification"), null, null, 1);` | Seul paramètre de requête émis par le produit : `source=app-notification`, constante. Il révèle au serveur qu'une sollicitation a été cliquée ; il ne porte ni identifiant, ni compteur, ni horodatage. | aucun trouvé |
| 3.4 | `src/StoreReview.cs:44, 49` | `var context = StoreContext.GetDefault();` … `var operation = context.RequestRateAndReviewAppAsync();` | Le seul appel sortant **non déclenché par un clic sur un lien** : l'API Store de Windows, qui parle au service Microsoft avec l'identité Store de l'utilisateur. Déclenchée par (a) le clic sur la sollicitation d'avis, (b) « Noter sur le Microsoft Store » du menu, (c) la fermeture d'un partage réussi (`MaybeShowReviewAfterShare`, cf. §4). Rien ne transite du produit vers l'association. | `ReviewSharePromptTests.*` couvrent la décision, pas l'appel |
| 3.5 | `src/StoreReview.cs:81, 90` | `Win32.ShellExecuteW(IntPtr.Zero, "open", fallbackUrl, null, null, 1);` | Repli : ouverture de `ms-windows-store://review/?ProductId=9N4BTS43SSSZ`. Le repli est déclenché **sur un thread de pool COM**, hors thread d'interface. | aucun trouvé |
| 3.6 | `src/AboutWindow.cs:340, 343` | `if (code == 0) OpenLink("https://eupl.eu/1.2/fr/");`<br>`if (code == 0) OpenLink("https://www.helloasso.com/associations/association-pour-la-modernisation-du-clavier-francais");` | Deux URL littérales hors `ProductIdentity` : la licence et la page HelloAsso de l'association. La page HelloAsso n'est **pas** gardée par `PolicyManager.ExternalLinksEnabledNow` ni par `AppChannel.CurrentIsSober` à ce niveau (la garde de canal est portée par le menu, pas par la fenêtre À propos). | aucun trouvé |
| 3.7 | `src/UsageStatsWindow.cs:334, 337` | `Win32.ShellExecuteW(IntPtr.Zero, "open", ProductIdentity.Url("/feedback"), null, null, 1);` | La fenêtre de statistiques ouvre `/feedback` et le lien Discord. | aucun trouvé |
| 3.8 | — | — | **Ce qui pourrait contredire « aucune télémétrie », sans conclure** : (a) l'appel `RequestRateAndReviewAppAsync` transmet à Microsoft le fait que cette installation a ouvert la boîte de notation ; (b) `?source=app-notification` signale au serveur du projet qu'une sollicitation a été cliquée ; (c) le PDF et les pages du site sont servis par l'infrastructure du projet, donc chaque ouverture laisse une trace serveur (IP, UA, `Referer`) hors du contrôle du binaire ; (d) l'invitation Discord est un identifiant d'invitation unique et mesurable côté Discord. Aucun de ces quatre n'est un envoi de données du produit. | aucun trouvé |

**Correction minimale possible** — 3.6 : router le lien HelloAsso par `PolicyManager.ExternalLinksEnabledNow` comme les entrées de menu. 3.8(b) : documenter `source=app-notification` dans la page de confidentialité.

---

## 4. Sollicitation d'avis

Deux chemins **indépendants**, avec deux jeux de gardes distincts, un compteur commun (`reviewPromptCount`, plafond 2) et une date commune (`reviewPromptLastShown`).

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 4.1 | `src/TrayApplication.cs:94-106` | `private const int ReviewPromptFirstActiveDays = 3;`<br>`private const int ReviewPromptSecondActiveDays = 10;` | Chemin **notification** : essai 1 à ≥ 3 jours d'usage distincts **et** ≥ 3 jours calendaires depuis `firstRemapDate` ; essai 2 à ≥ 10 jours d'usage distincts, ≥ 7 jours après l'essai 1, et dernière activité ≤ 3 jours (`ReviewPromptStaleDays`). Silence de 48 h après toute erreur journalisée (`ReviewPromptErrorCooldownHours`). | `DailyChallengeTests.*` pour l'interaction, aucun test direct de `MaybeShowReviewPrompt` (méthode privée d'instance) |
| 4.2 | `src/TrayApplication.cs:1943-1956` | `if (!PolicyManager.ExternalLinksEnabledNow) return false;`<br>`if (!ConfigManager.NotificationsEnabled) return false;` | Ce chemin tombe avec les liens externes (donc éteint sur le canal AMCF, et éteignable par stratégie sur le canal Store) **et** avec le réglage de notifications. Il n'est appelé qu'à deux moments : ctor (`:307`) et fermeture de la fenêtre d'accueil (`:1092`, via `_reviewPromptDeferred`). Il **ne peut donc pas** tirer pendant une partie ou en cours de session : il ne tire qu'au démarrage du processus. | `PolicyTests.LiensExternes_EteintsParPolitique_MemeSurStore` (`:236`) pour la règle de précédence |
| 4.3 | `src/ReviewSharePrompt.cs:48, 54, 64-83` | `public const int MaxPrompts = 2;` / `public const int MinActiveDays = 2;`<br>`if (s.Channel != DistributionChannel.Store) return false;` | Chemin **partage** : canal Store exclusivement, liens externes permis, jamais après un clic, plafond 2, silence 48 h, une par jour sur la date persistée, `firstRemapDate` non nulle, ≥ 2 jours d'usage distincts. Seuil **différent** de celui du chemin notification (2 vs 3/10), et décidé à part (`:50-53`). Il ne consulte **pas** `ConfigManager.NotificationsEnabled` : une personne ayant coupé les notifications reçoit quand même la boîte Store après un partage (documenté comme voulu, `TrayApplication.cs:2083-2084`). | `ReviewSharePromptTests` × 12 (`:60-164`), `PolicyTests.Partage_LiensExternesEteints_NeSollicitePas` (`:367`) |
| 4.4 | `src/ConfigManager.cs:358` | `public static int ReviewPromptCount => (int)GetUInt("reviewPromptCount");` | Migration `reviewPromptDone` : cf. 2.6. Effet daté du commit `d356841` : une installation 1.1.0 qui avait vu sa sollicitation repart avec **deux** essais. La contrepartie (l'utilisateur v1.1 qui a noté est indistinguable de celui qui a ignoré) est écrite dans le code, `:349-356`. | `ReviewPromptConfigTests.ReviewPromptCount_LegacyDoneFlagNoLongerConsumesAnAttempt` (`:141`) |
| 4.5 | `src/TrayApplication.cs:1984-1987` | `var lastShown = ConfigManager.ReviewPromptLastShown;`<br>`if (lastShown.HasValue && today.DayNumber - lastShown.Value.DayNumber < ReviewPromptSecondMinGapDays)` | Sur une installation migrée de la 1.1, `reviewPromptLastShown` est absente : le plancher de 7 jours entre les deux essais est **sauté** au premier passage (commentaire assumé `:1982-1983`). Le plafond de deux essais et le seuil de 10 jours d'usage restent. | aucun trouvé |
| 4.6 | `src/TrainingReminders.cs:57` | `if (s.ReviewPromptLastShown == today) return false;     // l'avis J+7 prime` | Priorité sur le rappel du Défi du jour : lue sur la **date persistée**, donc elle survit au redémarrage du processus. | `DailyChallengeTests.NoReminder_WhenReviewPromptShownToday` (`:133`), `…Snapshot_ReadsReviewPromptDate_FromPersistedConfig` (`:212`), `…ReviewPriority_SurvivesProcessRestart` (`:223`) |
| 4.7 | `src/AppChannel.cs:96-97` | `internal static bool IsSober(DistributionChannel channel) =>`<br>`channel == DistributionChannel.Amcf;` | Canal AMCF sobre : aucune sollicitation, ni par notification (4.2 via `ExternalLinksEnabledNow`), ni par partage (4.3 via le test explicite de canal). Hors package : `ShouldPrompt` rend faux, et `MaybeShowReviewPrompt` vise la page `/feedback` et non le Store (`TrayApplication.cs:1997`). | `SoberChannelTests.CanalSobre_EstLeCanalAmcfEtLuiSeul` (`:87`), `ReviewSharePromptTests.ShouldPrompt_CanalAmcf_ReturnsFalse` (`:75`) |
| 4.8 | `src/TrayApplication.cs:2110, 2125` | `if (!StoreReview.TryShow(_hWnd, StoreReviewUrl)) return;` … `ConfigManager.RecordReviewPromptShown(signals.Today);` | Le chemin partage ouvre une **boîte modale du Store** dans l'application. Déclencheur : fermeture de la fenêtre Leçons après une copie de résultat (`OnChallengeShared`) ou de statistiques (`OnStatsShared`). Il n'existe **aucune** garde de premier plan : ni `_suspendedForCompatibility`, ni `IsPaused`, ni `GameRegistry.IsRemoteAccessProcess`. En pratique le geste est volontaire et la fenêtre Leçons est au premier plan ; le cas d'un jeu en fenêtré sans bordure repassant devant entre la copie et la fermeture n'est pas couvert. | aucun trouvé |
| 4.9 | `src/TrayApplication.cs:2060-2064` | `private void OnRateStoreFromMenu()` … `try { ConfigManager.SetReviewPromptClicked(); }` | « Noter sur le Microsoft Store » depuis le menu pose `reviewPromptClicked` (plus aucune relance) **sans** consommer d'essai. | `SoberChannelTests.NoterSurLeStore_PresenteSurStore` (`:63`) / `…AbsenteSurAmcf` (`:71`) |
| 4.10 | `src/UsageStats.cs:426-427` + `src/TrayApplication.cs:1971` | `internal static bool CollectionEnabled =>`<br>`PolicyManager.UsageStatsEnabled(PolicyManager.Current.UsageStats, AppChannel.Current);` | Interaction non écrite ailleurs : quand la stratégie `UsageStatsEnabled=0` s'applique sur le canal Store, `usage-stats.json` n'est plus ni lu ni écrit, donc `ActiveDaysCount` ne dépasse jamais 1 dans une session — les seuils de 3 et 10 jours ne sont **jamais** atteints et la sollicitation d'avis ne tire plus jamais, y compris par partage (`MinActiveDays = 2`). | `StatsCollectionTests.SurAmcf_LesCompteursVivantEnMemoire` (`:98`) décrit le mécanisme, aucun test ne relie les deux |

**Correction minimale possible** — 4.8 : ajouter `if (_suspendedForCompatibility || IsPaused) return;` en tête de `MaybeShowReviewAfterShare`. 4.10 : mentionner l'effet de bord dans l'explication ADML de `UsageStatsEnabled`.

---

## 5. Politiques d'entreprise

Racine unique, **HKLM seulement** : `PolicyManager.cs:73, 76` — `PoliciesRoot = @"SOFTWARE\Policies\"` + `ProductIdentity.Namespace` → `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\AZERTYGlobal`, vue 64 bits forcée (`RRF_SUBKEY_WOW6464KEY`, `:278`). **Aucune** lecture HKCU. Lecture une seule fois, mise en cache (`:97-123`) : un changement de stratégie exige un redémarrage de l'application.

| Clé dans le code (`src/PolicyManager.cs`) | Clé dans l'ADMX (`entreprise/AZERTYGlobal.admx`) | Clé dans le `.reg` (`entreprise/politiques-exemple.reg`) | Écart |
|---|---|---|---|
| `ValueNotifications = "NotificationsEnabled"` (`:78`) | `valueName="NotificationsEnabled"` (`:39`), activé=1 / désactivé=0 | `"NotificationsEnabled"=dword:00000000` (`:24`) | aucun |
| `ValueUsageStats = "UsageStatsEnabled"` (`:79`) | `valueName="UsageStatsEnabled"` (`:49`), 1/0 | `"UsageStatsEnabled"=dword:00000000` (`:29`) | aucun |
| `ValueExternalLinks = "ExternalLinksEnabled"` (`:80`) | `valueName="ExternalLinksEnabled"` (`:59`), 1/0 | `"ExternalLinksEnabled"=dword:00000000` (`:33`) | aucun |
| `ValueShowOnboarding = "ShowOnboarding"` (`:81`) | `policy name="RemoveOnboarding"` mais `valueName="ShowOnboarding"` (`:68-76`), **activé=0 / désactivé=1** | `"ShowOnboarding"=dword:00000000` (`:38`) | nom de stratégie ≠ nom de valeur, **inversion assumée** et documentée aux trois endroits (`admx:12-16`, `adml fr:27-28`, `reg:11-13`) |
| `ValueLanguage = "Language"` (`:86`) | `<enum id="Language" valueName="Language" required="true">` (`:86`), items `fr` / `en` | `"Language"="fr"` (`:42`) | aucun |
| `KeyPath = "SOFTWARE\Policies\" + "AZERTYGlobal"` (`:73, 76`) | `key="SOFTWARE\Policies\AZERTYGlobal"` (× 5) | `[HKEY_LOCAL_MACHINE\SOFTWARE\Policies\AZERTYGlobal]` (`:19`) | aucun |

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 5.1 | `src/PolicyManager.cs:134-139` | `private static bool? ReadFlag(PolicyValueReader reader, string valueName) =>`<br>`reader(KeyPath, valueName) switch { int dword => dword != 0, _ => null, };` | Un REG_DWORD non nul autre que 1 vaut vrai. Une valeur posée dans un **autre type** (REG_SZ pour un flag) rend `null` : la stratégie ne s'applique pas, et rien n'est tracé pour ces quatre valeurs-là — seule `Language` journalise son rejet (`:154-155`). | `PolicyTests.Read_DwordNonNul_RendTrue` (`:133`), `…Read_DwordAvecTypeInattendu_RendNull` (`:140`) |
| 5.2 | `src/PolicyManager.cs:230-231` | `internal static bool ShowOnboarding(bool? policy, bool userSetting) =>`<br>`policy == false ? false : userSetting;` | Seule des cinq à ne pas verrouiller à 1 ; l'ADML fr `:28` dit exactement cela. | `PolicyTests.FenetreDeBienvenue_PolitiqueAUn_NImposeRien` (`:253`), `…PolitiqueAZero_Supprime` (`:260`), `…NEstGriseeQueParUnZero` (`:288`) |
| 5.3 | `entreprise/fr-FR/AZERTYGlobal.adml:22` | `l'application tient ses compteurs dans %LocalAppData%\AZERTY Global\usage-stats.json` | Le chemin annoncé à la DSI est le chemin **non redirigé**. Sous MSIX le fichier est à `…\Packages\AZERTYGlobal.AZERTYGlobal_w9kghr08zmhbg\LocalCache\Local\AZERTY Global\` (mesuré, `docs/audit-2026-09-15-v1.2.0/recette-vm-resultats.md:51`), et c'est cette forme-là que retient `entreprise/Note RGPD - Établissements.md:27`. Même écart dans le `.reg` `:26` et dans l'ADML en-US `:26`. | `scripts/tests/test_admx_agreement.py` compare les **noms de valeurs**, pas les textes explicatifs |
| 5.4 | `entreprise/politiques-exemple.reg` (octets `ff fe`) | `fffe 5700 6900 6e00 6400 6f00 7700 7300` (`xxd -l 16`) | UTF-16LE avec BOM : forme acceptée par `reg import` / regedit. L'ADMX et les deux ADML sont en UTF-8 sans BOM, conforme à leur déclaration `encoding="utf-8"`. | aucun trouvé |
| 5.5 | `src/PolicyManager.cs:110-118` | `catch (Exception ex) { … _cache = PolicySet.None; }` | Registre illisible → aucune politique, comportement de poste non géré (direction d'échec écrite `:112-115`). | `PolicyTests.Current_NeLitLeRegistreQuUneFois` (`:452`), `…ReadRegistryValue_CleInexistante_RendNull` (`:493`) |

**Correction minimale possible** — 5.3 : remplacer le chemin par la forme `LocalCache\Local` dans les deux ADML et le `.reg`, ou ajouter une phrase de redirection MSIX. 5.1 : journaliser un `PolicyValueIgnored` aussi pour un flag de type inattendu.

---

## 6. Démarrage automatique

Deux mécanismes, **jamais la clé `Run` du registre** : grep de `CurrentVersion\Run`, `RegSetValue` et `Microsoft.Win32.Registry` sur tout `src/` → aucun résultat. La question de la virtualisation de `HKCU\…\Run` sous MSIX ne se pose donc pas ici.

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 6.1 | `src/AutoStart.cs:98-111` | `if (ConfigManager.IsPackaged) { if (!SetStartupTask(enabled)) return false; }`<br>`else { if (enabled) { if (!Enable()) return false; } else Disable(); }` | Packagé : `StartupTask` WinRT. Hors package : raccourci `.lnk` créé par `IShellLink`/`IPersistFile` en P/Invoke pur (vtable lue à la main, `:161-207`) dans `Environment.SpecialFolder.Startup`, nommé `AZERTY Global.lnk` (`ProductIdentity.cs:36`). | `QuickWinsTests.StartupTaskEnabledByPolicy_IsReportedAsActive` (`:42`) |
| 6.2 | `src/AutoStart.cs:238` vs `msix/AppxManifest.xml:54` | `private const string StartupTaskId = "AZERTYGlobalStartup";` / `TaskId="AZERTYGlobalStartup"` | Identifiants **identiques**. Le manifeste déclare `Enabled="false"` (`:55`) : le lancement automatique est éteint à l'installation. `Executable`/`EntryPoint` de l'extension reprennent ceux de l'application. | aucun test ne compare ces deux littéraux (contrairement au CLSID de toast, cf. 7.1) |
| 6.3 | `src/AutoStart.cs:128-129` | `public static bool IsRegistered =>`<br>`ConfigManager.IsPackaged ? IsStartupTaskEnabled() : File.Exists(ShortcutPath);` | Détection de l'état lue **à la source** (état de la tâche, ou existence du fichier) et non dans `config.json` ; `autoStartEnabled` de la configuration n'est qu'un cache de compatibilité (`ConfigManager.cs:177`). Toute exception rend faux (`:301`). | aucun trouvé |
| 6.4 | `src/AutoStart.cs:240-242` | `private static bool IsStartupTaskActive(Windows.ApplicationModel.StartupTaskState state) =>`<br>`state is …Enabled or …EnabledByPolicy;` | `DisabledByUser` (bascule refusée dans Paramètres › Applications › Démarrage) et `DisabledByPolicy` sont traités comme « inactif » : `Set(true)` rend faux et l'utilisateur voit `AutoStart_FailureMessagePackaged`, qui le renvoie au bon écran Windows. | `QuickWinsTests.StartupTaskEnabledByPolicy_IsReportedAsActive` (`:42`) |
| 6.5 | `src/AutoStart.cs:113-117` | `bool finalState = IsRegistered;`<br>`if (finalState != enabled) return false;` | L'état final est **relu** après l'opération avant d'être mis en cache : un `RequestEnableAsync` qui rend `Enabled` mais que le système refuse ensuite ne peut pas mentir au cache. | aucun trouvé |
| 6.6 | `src/AutoStart.cs:257-263` | `// Déjà activée : ne PAS rappeler RequestEnableAsync.` … `if (IsStartupTaskActive(task.State)) return true;` | Idempotence voulue, motivée par un faux échec observé pendant un `WM_CLOSE` de mise à jour MSIX (smoke test 2026-07-20). | aucun trouvé |

**Correction minimale possible** — 6.2 : ajouter un test miroir de `ActivatorClsid_MatchesAppxManifestDeclaration` qui lit `TaskId` du manifeste et le compare à `AutoStart.StartupTaskId`.

---

## 7. Activation des toasts

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 7.1 | `src/ToastActivation.cs:54` vs `msix/AppxManifest.xml:71, 79` | `internal const string ActivatorClsidString = "126A58B4-3200-43A6-9018-612C108F4A94";` / `com:Class Id="126A58B4-…"` / `ToastActivatorCLSID="126A58B4-…"` | Les **trois** valeurs sont identiques, et l'égalité est tenue par un test qui lit réellement le manifeste. | `ToastActivationTests.ActivatorClsid_MatchesAppxManifestDeclaration` (`:89`), `…ClsidDivergent_EstRapporteTelQuel` (`:118`) |
| 7.2 | `src/TrayApplication.cs:252-257` | `if (args.Contains("action=review", StringComparison.Ordinal))`<br>`Win32.PostMessageW(_hWnd, WM_APP_TOAST, args.Contains("target=feedback", …) ? (IntPtr)2 : (IntPtr)1, IntPtr.Zero);` | Analyse des arguments : **deux** sous-chaînes cherchées, `action=review` (+ `target=feedback`) et `action=autostart`. Rien d'autre n'est lu. L'argument est immédiatement réduit à un entier 1, 2 ou 3 avant de quitter le thread RPC. | `ToastActivationTests.EscapeXml_*` (`:17, 24`) pour l'émission ; aucun test de l'analyse côté réception |
| 7.3 | `src/TrayApplication.cs:599-607` | `case WM_APP_TOAST:` … `if (wParam == (IntPtr)1) OpenReviewTarget(toStore: true);` | Ce qui peut être lancé depuis un argument : la boîte de notation Store, `ms-windows-store://review/?ProductId=9N4BTS43SSSZ`, `https://azerty.global/feedback?source=app-notification`, ou l'activation du démarrage automatique. **Aucune URL, aucun chemin et aucun exécutable ne provient de l'argument** : les trois cibles sont des constantes de compilation. | aucun trouvé |
| 7.4 | `src/ToastActivation.cs:76-80` | `public void Activate(string appUserModelId, string? invokedArgs, IntPtr data, uint count)`<br>`{ try { Activated?.Invoke(invokedArgs ?? ""); }` | `appUserModelId` est reçu et **jamais vérifié**. La fabrique est enregistrée en `CLSCTX_LOCAL_SERVER | REGCLS_MULTIPLEUSE` (`:122-124`) : tout processus de la même session utilisateur qui connaît le CLSID peut `CoCreateInstance` puis appeler `Activate("action=autostart")` ou `("action=review&target=store")`. Surface atteignable : les trois actions de 7.3, rien de plus. | aucun trouvé |
| 7.5 | `src/ToastActivation.cs:184-196, 211-216` | `$"<toast launch=\"{EscapeXml(launchArgs)}\" activationType=\"foreground\">"` … `.Replace("&", "&amp;")` | Le XML du toast est construit par concaténation, mais **toutes** les valeurs interpolées passent par `EscapeXml` (5 entités). Les valeurs sont de toute façon des constantes ou des chaînes de `Localization/`. `data`/`count` (saisie utilisateur du toast) sont ignorés : aucun champ de saisie n'est déclaré. | `ToastActivationTests.EscapeXml_NeutralizesMarkupCharacters` (`:17`) |
| 7.6 | `src/Program.cs:41-56` | `if (!ConfigManager.IsPackaged) { Win32.MessageBoxW(…L.Startup_AlreadyRunning…); } return;` | Repli quand l'activateur COM n'est pas enregistré : Windows relance l'exe, la seconde instance meurt en silence en contexte packagé. L'action du toast est alors perdue (l'instance vivante l'a reçue par `NIN_BALLOONUSERCLICK` dans le cas balloon, pas dans le cas toast). | `QuickWinsTests.SecondInstanceLog_DoesNotContainRawArguments` (`:57`) |

**Correction minimale possible** — 7.4 : comparer `appUserModelId` au AUMID du paquet (`AZERTYGlobal.AZERTYGlobal_w9kghr08zmhbg!AZERTYGlobal`) et ignorer l'activation sinon.

---

## 8. Localisation

Socle sans `.resx` : chaque chaîne porte ses deux variantes dans un seul appel `T(fr, en)` (`Localization/L.cs:25`). Il n'existe donc **pas d'identifiant** pouvant être orphelin d'un côté : une omission prend la forme d'un couple identique, pas d'une clé manquante. 972 lignes réparties en 16 fichiers partiels.

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 8.1 | `src/Localization/L.cs:25` | `private static string T(string fr, string en) => IsEnglish ? en : fr;` | Repli : aucun. `L.Language` vaut « fr » ou « en », toute autre valeur est refusée en amont (`ConfigManager.cs:280`, `raw == "en" ? "en" : "fr"`). Une chaîne non traduite est structurellement impossible à manquer au runtime. | `LocalizationTests.AppLanguage_CorruptValue_FallsBackToFr` (`:89`), `…L_IsEnglish_ReflectsLanguage` (`:97`) |
| 8.2 | `src/Localization/L.LessonsWindow.cs:23` | `public static string LessonsWin_BtnResetStats => T("Reset stats", "Reset stats");` | Couple identique dont la version « française » est de l'anglais : le bouton affiche « Reset stats » en FR. Idem `:21` `T("Reset stats libre", "Reset free stats")` — franglais côté FR. Les autres couples identiques relevés sont légitimes (`"Actions"`, `"Minutes"`, `"Notifications"`, `"Total"`, `"Applications (*.exe)"`, `" (Bonus)"`). | aucun trouvé (`LocalizationTests` ne parcourt pas les tables) |
| 8.3 | `src/Program.cs:23` | `L.Language = ConfigManager.AppLanguage;` | Ordre de résolution : **stratégie** (`PolicyManager.AppLanguage`, `ConfigManager.cs:266-267`) > **réglage utilisateur** (`appLanguage` de `config.json`) > défaut. La langue est lue en tout premier, avant le mutex, donc avant tout message affichable. | `PolicyTests.Langue_SousPolitique_PrimeSurLeChoixUtilisateur` (`:266`), `…SetAppLanguage_SousPolitique_NEcritRien` (`:396`) |
| 8.4 | `src/ConfigManager.cs:287-288, 954-962` | `internal static string DefaultAppLanguage(ushort windowsUiLangId) =>`<br>`(windowsUiLangId & 0x3FF) == 0x0C ? "fr" : "en";` | Langue **système** consultée **uniquement** sur une installation neuve (aucun fichier `config.json`), et le résultat est figé immédiatement. Une configuration existante sans la clé reste en « fr » : une mise à jour ne bascule jamais de langue. | `LocalizationTests.InstallationNeuve_EcritLaLangueDeriveDeWindows` (`:31`), `…ConfigExistanteSansCle_ResteEnFrancais` (`:47`), `…DefaultAppLanguage_DeriveDuLangId` (`:65`) |
| 8.5 | `src/ConfigManager.cs:299-309` | `public static void SetAppLanguage(string lang)` … `if (PolicyManager.LanguageIsManagedNow) return;` | Porte unique : le verrou de stratégie est posé dans le setter, pas dans les trois fenêtres qui changent la langue. Le menu de la zone de notification grise en plus son entrée (`TrayApplication.cs:1461`). | `PolicyTests.SetAppLanguage_SousPolitique_NEcritRien` (`:396`) |
| 8.6 | `src/ConfigManager.cs:920` | `throw new JsonException("La configuration doit être un objet JSON.");` | Chaînes françaises **codées en dur hors tables** dans le périmètre, liste exhaustive : ce message d'exception ; `LessonProgressStore.cs:190` `"Format de progression non pris en charge ; fichier conservé."` ; `ConfigManager.cs:972` et `LessonProgressStore.cs:248` `"Sauvegarde ignoree: … non charge."`. Les quatre finissent dans `error.log`, jamais à l'écran. Les autres occurrences relevées par le balayage (`TrayApplication.cs:1883-1910`, `DailyChallenge.cs:51-55`) sont des **glyphes**, pas du texte. | aucun trouvé |
| 8.7 | `src/UsageStats.cs:353-354` | `if (hours >= 100) return L.IsEnglish ? $"{hours} hr" : $"{hours} h";` | Format d'heure composé à la main plutôt que via `L`, mais les deux variantes existent. `L.FormatDate` (`L.cs:28-39`) retombe sur `yyyy-MM-dd` si la culture est introuvable — cas atteignable puisque `InvariantGlobalization` est à `false` (`csproj:27`) mais qu'un conteneur sans ICU rendrait `CultureNotFoundException`. | `UsageStatsTests.FormatActiveTime_FormatsAsExpected` (`:306`), `…FormatActiveTime_English_UsesHrMin` (`:312`) |

**Correction minimale possible** — 8.2 : `T("Réinitialiser les stats", "Reset stats")` et `T("Réinitialiser les stats du mode libre", "Reset free stats")`.

---

## 9. Statistiques d'usage — ce qui est persisté

Fichier unique `usage-stats.json`, même dossier que `config.json`. **Aucune rétention, aucune purge** : les compteurs sont cumulatifs sur la vie de l'installation.

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 9.1 | `src/UsageStats.cs:532-545` | `writer.WriteNumber("accentedUppercaseCount", _accentedUppercaseCount);` | Contenu **exhaustif** du fichier, 14 clés : `firstRemapDate`, `lastActiveDate`, `lastSpecialCharDate` (trois dates au **jour** près, `yyyy-MM-dd`), `activeDaysCount`, `currentStreak`, `bestStreak`, `totalActiveMinutes`, et sept compteurs entiers : `accentedUppercaseCount`, `frenchTypographyCount`, `internationalCount`, `symbolsCount`, `searchOpenCount`, `virtualKeyboardOpenCount`, `challengesCompletedCount`. | `UsageStatsTests.Flush_PersistsDirtyState_SurvivesReload` (`:219`), `…TotalActiveMinutes_SurvivesFlushAndReload` (`:290`) |
| 9.2 | `src/UsageStats.cs:182-192` | `foreach (char c in text) { if (!TryCategorize(c, out var bucket)) continue;` … `case Category.AccentedUppercase: _accentedUppercaseCount++; break;` | Le texte émis est parcouru caractère par caractère, mais **seul le compteur de sa catégorie** est incrémenté : ni le caractère, ni son code, ni sa position, ni la séquence ne sont conservés. Quatre compteurs pour l'ensemble de l'alphabet remappé. Aucune séquence, aucun n-gramme, aucun horodatage par frappe. | `UsageStatsTests.Categorization_FourCategories_CountedSeparately` (`:111`) et 4 autres cas de catégorisation (`:125-160`) |
| 9.3 | `src/UsageStats.cs:33-37` | `// Minutes distinctes avec au moins une frappe remappée, cumulées.`<br>`private static long _totalActiveMinutes;` | La granularité temporelle la plus fine persistée est la **minute comptée**, et seul le **total** l'est : `_lastActiveMinute` (`:37`) n'est jamais écrit. Aucune distribution horaire, donc aucun moyen de reconstituer *quand* la personne tape. | `UsageStatsTests.RecordEmittedText_SameMinute_CountsOneActiveMinute` (`:278`) |
| 9.4 | `src/UsageStats.cs:162-167` | `/// … DOIT rester in-memory uniquement (aucune I/O ici) : le hook clavier`<br>`/// est sur le chemin critique de la frappe.` | Aucune écriture sur le chemin du hook. Persistance différée : timer de 5 min (`TrayApplication.cs:461-462, 821`) et à la fermeture (`:1787`). Une fermeture brutale perd au plus 5 minutes de compteurs. | `UsageStatsTests.Flush_WriteFailure_RetriesWithoutRequiringAnotherKeystroke` (`:243`) |
| 9.5 | `src/UsageStats.cs:366-405` | `public static string BuildShareText()` | Ce qui part dans un partage de **statistiques** : **texte brut** uniquement, copié dans le presse-papiers à l'initiative de l'utilisateur (`:363-364`, « Aucun envoi automatique »). Contenu : date de première frappe, nombre de jours actifs, temps actif cumulé et moyenne par jour, total de caractères spéciaux et sa ventilation en quatre catégories. Aucun identifiant, aucun nom de machine, aucun nom de processus, aucune image. | `UsageStatsTests.BuildShareText_WithActivity_ContainsCountsAndDate` (`:349`), `…BuildShareText_NoActivity_…` (`:341`) |
| 9.6 | `src/ChallengeShare.cs:42-45` | `var hardest = stats.GetHardestCharacters(HardestCharacterCount);`<br>`lines.Add(hardest.Count == 0 ? L.Challenge_ShareFlawless : L.Challenge_ShareHardest(…));` | Ce qui part dans un partage de **défi** : texte brut, date du défi, vitesse, précision, durée, mention de record, **trois caractères les plus difficiles** et attribution de l'extrait. Ces trois caractères proviennent de l'exercice du jour, identique pour tout le monde, jamais de la frappe libre de l'utilisateur. Aucune statistique d'usage n'y figure, et un test le tient. | `ChallengeShareTests.Build_CarriesNoUsageStatistics` (`:96`), `…Build_WithErrors_ListsTheHardestCharacters` (`:59`) |
| 9.7 | `src/LessonProgressStore.cs:220-227, 269-279` | `if (el.TryGetProperty("errorMatrix", out _)) legacyErrorMatrixFound = true;` … `if (legacyErrorMatrixFound) Save(…);` | `lessons-progress.json` : la matrice d'erreurs par caractère d'une version antérieure est **détectée puis supprimée** à la réécriture. Ce qui reste par exercice : `hash`, `completed`, `successfulAttempts`, `bestWpm`, `bestAccuracyPercent`, `bestSeconds`, `lastCompletedUtc` (horodatage **à la seconde**, seul de tout le périmètre), `hintsUsed`. | `LessonCoreTests` (652 lignes) ; aucun test ne nomme la suppression de `errorMatrix` |
| 9.8 | `src/UsageStats.cs:439-448` | `if (!CollectionEnabled) return;` (dans `EnsureLoaded`) et `:519` `if (!CollectionEnabled) return true;` (dans `SaveLocked`) | Collecte éteinte (canal AMCF par défaut, ou stratégie `UsageStatsEnabled=0`) : le fichier n'est **ni lu ni créé**. Les compteurs vivent quand même en mémoire pendant la session et disparaissent à la fermeture. | `StatsCollectionTests.SessionComplete_SurAmcf_NeCreeAucunFichier` (`:66`), `…SurAmcf_NeRelitPasUnFichierExistant` (`:111`), `…SurStore_RelitLeFichierExistant` (`:124`) |
| 9.9 | `src/ConfigManager.cs:561-575` | `_compatibilityCache[processName] = mode;` | Le seul élément persisté de tout le périmètre qui identifie ce que fait l'utilisateur de son poste est la liste des **noms de processus** pour lesquels il a posé un override de compatibilité, en clair dans `config.json` (cf. 2.10). Il n'y en a que si l'utilisateur en pose. | `ConfigManagerCompatTests.SetCompatibilityOverride_ForceOn_PersistsAndReadsBack` (`:35`) |

---

## 10. Défi du jour, rappels, notifications

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 10.1 | `src/TrayApplication.cs:461-462, 818-825` | `private const uint STATS_FLUSH_INTERVAL_MS = 5 * 60_000;` … `MaybeShowTrainingReminder();` | Le rappel d'entraînement est **évalué toutes les 5 minutes**, sur le même timer que le vidage des statistiques. Décision pure, sans I/O au-delà des lectures de configuration. Autres timers permanents : watchdog de hook 60 s, watchdog de premier plan **250 ms**. | `DailyChallengeTests.*` (`:117-186`) couvrent la décision, pas la cadence du timer |
| 10.2 | `src/TrainingReminders.cs:29, 33, 36, 40` | `public const uint MaxIgnored = 3;` / `public const int EarliestHour = 17;`<br>`public const int StaleSpecialCharDays = 3;` / `public const long HelperOpensThreshold = 20;` | Plafonds : opt-in explicite (`trainingEnabled`, faux par défaut), **un rappel par jour** au plus, **jamais avant 17 h** locales, arrêt **définitif** après 3 rappels expirés sans clic, aucun rappel le jour d'une sollicitation d'avis, aucun le jour d'une séance déjà faite. | `DailyChallengeTests.NoReminder_WhenDisabledOrStopped` (`:117`), `…BeforeEveningWindow` (`:125`), `…TwiceSameDay_OrAfterTodaysSession` (`:143`) |
| 10.3 | `src/TrainingReminders.cs:109, 113-114` | `public static void MarkReminderClicked() => ConfigManager.SetTrainingIgnoredCount(0);`<br>`MarkReminderIgnored() => …(ConfigManager.TrainingIgnoredCount + 1);` | Le compteur d'ignorés est alimenté par `NIN_BALLOONTIMEOUT` (`TrayApplication.cs:593-594`) : il ne fonctionne **que** sur le canal balloon. Le rappel d'entraînement n'emprunte jamais le canal toast (`MaybeShowTrainingReminder` appelle `ShowBalloon` directement, `:1265`), donc la garde tient. | `DailyChallengeTests.Reminds_WhenStreakInDanger` (`:151`) |
| 10.4 | — (grep) | `SHQueryUserNotificationState`, `QUNS_*`, `ToastNotifier.Setting` : **0 occurrence** dans `src/` | Aucune consultation de l'Assistant de concentration, du mode présentation, du plein écran exclusif ni de l'état d'autorisation des notifications Windows. Le produit s'en remet entièrement à Windows pour retenir la notification, ce que Windows fait pour un toast mais **pas** pour une balloon `Shell_NotifyIcon` en contexte non packagé. Aucune heure silencieuse propre au produit, hors le plancher de 17 h du rappel d'entraînement (10.2), qui n'a pas de plafond horaire : un rappel peut sortir à 3 h du matin si l'application tourne. | aucun trouvé |
| 10.5 | `src/TrayApplication.cs:2144, 2214-2221` | `private void ShowBalloon(string title, string text) { if (!ConfigManager.NotificationsEnabled) return;` vs `ShowSecurityBalloon` sans cette garde | Deux canaux : les bulles de confort respectent le réglage **et** la stratégie ; les bulles de sécurité (anti-triche, accès distant, nettoyage d'override) les contournent délibérément (`:2209-2212`), ce que l'ADML annonce (« Les avertissements de sécurité s'affichent dans tous les cas »). | `PolicyTests.Notifications_SousPolitique_IgnoreLeReglageUtilisateur` (`:202`) |
| 10.6 | `src/Localization/L.Tray.cs:117-119` | `public static string Tray_ReviewPromptTitle(int attempt) => attempt >= 2`<br>`? T($"Votre avis sur {Product} ?", …) : T($"{Product} vous plaît ?", …);` | Gabarit : le nom du produit apparaît **exactement une fois** dans le couple titre + corps de chaque notification cliquable (avis : titre seul ; annonce du Défi : corps seul, `:182` ; relance d'autostart : titre seul, `:186`). En revanche, sur le canal **toast** (packagé), Windows ajoute lui-même l'attribution avec le `DisplayName` du manifeste — soit `AZERTY Global - Clavier français amélioré` (`msix/AppxManifest.xml:38`) : le nom se lit alors **deux** fois à l'écran, et sous une forme différente de celle du titre. | aucun trouvé |
| 10.7 | `src/ToastActivation.cs:168-172` | `public static string ToastBody(string body)` … `return i < 0 ? body : body[..i];` | Le corps du toast est tronqué à sa première ligne : la ligne « Cette notification ouvre… » du canal balloon est remplacée par le bouton d'action. Une seule chaîne sert les deux canaux. | `ToastActivationTests.UnCorpsDuCanalToast_NAQuUneLigneDAffordance` (`:158`), `…ToastBody_RetireLAffordance_EtGardeLeReste` (`:165`) |
| 10.8 | `src/TrayApplication.cs:1150-1156, 1183` | `ConfigManager.SetChallengeAnnounceDone(); // avant l'affichage : jamais deux fois,` | Annonce du Défi et relance d'autostart : **une seule fois sur la vie de l'installation**, marquées avant l'affichage pour qu'un échec d'émission ne rende pas un second essai possible. L'annonce du Défi ne consulte ni le canal ni `ExternalLinksEnabledNow` — elle n'ouvre rien d'externe. | `AutoStartNudgeTests.MarkPromptShown_PersistsAndSuppressesFurtherPrompts` (`:101`) |
| 10.9 | `src/TrayApplication.cs:307-311` | `else if (!MaybeShowReviewPrompt() && !MaybeShowChallengeAnnouncement()`<br>`&& !MaybeShowAutoStartNudge())` | Au démarrage, **au plus une** notification : avis, puis annonce du Défi, puis relance d'autostart, puis à défaut la bulle « Actif » différée de 1,5 s. Ordre de priorité fixé par le court-circuit. | `TrayUxRegressionTests` ne couvre pas cet ordre ; aucun test trouvé |

**Correction minimale possible** — 10.4 : appeler `SHQueryUserNotificationState` avant `ShowBalloon` et reporter au tick suivant si l'état vaut `QUNS_PRESENTATION_MODE`, `QUNS_RUNNING_D3D_FULL_SCREEN` ou `QUNS_QUIET_TIME`. 10.6 : raccourcir le `DisplayName` du manifeste à `AZERTY Global`, ou retirer le nom du titre des toasts.

---

## 11. Canaux de distribution

| # | fichier:ligne | citation | constat | test qui couvre |
|---|---|---|---|---|
| 11.1 | `src/AppChannel.cs:55-57` | `bool packaged = ConfigManager.IsPackaged;`<br>`ConfigManager.TryGetPackageFamilyName(out var familyName);` | Détection par **deux appels Windows indépendants** : `GetCurrentPackageFullName` pour l'existence d'un paquet, `GetCurrentPackageFamilyName` pour son identité. Le second n'est jamais utilisé pour déduire le premier, à dessein (`:50-54`). Détection unique au premier accès, non rafraîchie. | `AppChannelTests.Classify_*` × 5 (`:39-72`) |
| 11.2 | `src/AppChannel.cs:75-83` | `if (string.Equals(familyName, ProductIdentity.StorePackageFamilyName, StringComparison.OrdinalIgnoreCase))` … `return DistributionChannel.Amcf;` | Comparaison insensible à la casse contre `AZERTYGlobal.AZERTYGlobal_w9kghr08zmhbg`. Direction d'échec : **tout paquet inconnu, family name illisible compris, est traité comme le canal sobre** — jamais une sollicitation de trop. | `AppChannelTests.Classify_PackagedWithoutFamilyName_ReturnsAmcf` (`:63`), `…InAnotherCase_ReturnsStore` (`:72`), `…StorePackageFamilyName_MatchesTheInstalledPackage` (`:23`) |
| 11.3 | `src/TrayApplication.cs:1286-1299` | `PolicyManager.ExternalLinksEnabled(externalLinksPolicy, channel)`<br>`? new[] { IDM_SUPPORT, IDM_FEEDBACK, IDM_DISCORD, IDM_BUG } : new[] { IDM_FEEDBACK, IDM_BUG };` | Ce qui change par canal : le sous-menu « Retours et soutien » perd « Soutenir le projet » (`/soutien`) et Discord ; l'entrée de premier niveau « Noter sur le Microsoft Store » disparaît. « Donner mon avis » et « Signaler un bug » restent sur les trois canaux. Le menu est asservi à une liste que les tests lisent. | `SoberChannelTests` × 8 (`:25-87`), `PolicyTests.MenuRetours_*` (`:340, 350`) |
| 11.4 | `src/AppChannel.cs:96-97` + `src/PolicyManager.cs:217-225` | `internal static bool UsageStatsEnabled(bool? policy, DistributionChannel channel) =>`<br>`policy ?? !AppChannel.IsSober(channel);` | Deuxième différence par canal : la collecte de statistiques, éteinte par défaut sur AMCF (D5). Troisième : les deux chemins de sollicitation d'avis (§4). Les notifications de confort, elles, restent **actives** sur les trois canaux (`ConfigManager.cs:183-187`). | `StatsCollectionTests.CollectionEnabled_EteinteSurAmcf_ActiveAilleurs` (`:51`), `PolicyTests.Statistiques_PolitiqueUn_RallumeLaCollecteSurLeCanalSobre` (`:307`) |
| 11.5 | `src/AppChannel.cs:31-32, 91-94` | `/// <summary>Exécutable hors package. Comportement inchangé, décision D8.</summary>`<br>`Unpackaged,` | Le canal hors package garde **exactement** le comportement d'avant la v1.2.0 : liens externes visibles, statistiques collectées, sollicitation d'avis vers `/feedback` et non vers le Store. Le commentaire `:93-94` note que « le lot B du plan emploie pourtant cette formulation » (« ≠ Store »), c'est-à-dire que le plan et le code ne disent pas la même chose — le code est celui décrit ici. | `SoberChannelTests.HorsPackage_GardeLeMenuDAujourdhui` (`:80`), `AppChannelTests.Current_InTheTestHost_IsUnpackaged` (`:33`) |
| 11.6 | `src/AppChannel.cs:111-116` | `internal static IDisposable OverrideForTests(DistributionChannel channel)` | Le canal est forçable en test par un scope qui restaure l'état précédent **même sur exception**, et `UsageStats.CollectionEnabled` relit la valeur à chaque appel plutôt que de la mettre en cache (`UsageStats.cs:422-424`) précisément pour éviter la dépendance à l'ordre des tests. | `AppChannelTests.OverrideForTests_RestoresEvenWhenTheBodyThrows` (`:99`) |

---

## Non vérifié

- **Exécution réelle** : aucun binaire lancé, aucun MSIX installé, aucune clé de registre lue sur ce poste. Tout constat de comportement au démarrage, de redirection MSIX ou d'affichage de toast vient de la lecture du code et, pour la redirection, de la recette VM datée du 2026-09-19 (`docs/audit-2026-09-15-v1.2.0/recette-vm-resultats.md:51`), pas d'une mesure faite ici.
- **Suite de tests non exécutée** : les noms de tests cités sont lus dans les fichiers, leur verdict actuel n'est pas mesuré (`dotnet test` non lancé). Un test cité peut être rouge ou ignoré.
- **1.1 (mutex `Local\`)** : le comportement à deux sessions du même compte est déduit de la sémantique de l'espace de noms `Local\`, non observé. Un essai console + RDP trancherait.
- **7.4 (appel de `Activate` par un tiers local)** : la portée réelle de `CoRegisterClassObject` pour une application packagée n'a pas été testée ; un paquet MSIX peut restreindre la visibilité du serveur COM d'une manière que la lecture du code ne montre pas.
- **10.6 (nom du produit deux fois sur un toast)** : l'attribution ajoutée par Windows est le comportement documenté de `ToastGeneric` en contexte packagé ; non observée à l'écran ici.
- **Hors périmètre, donc non instruit** : rendu GDI des fenêtres, `KeyMapper`/`KeyboardHook`/`TypingEngine.*` (sauf les trois sites de journalisation cités), `SettingsWindow`, `OnboardingWindow`, `LessonsWindow`, `UsageStatsWindow` au-delà de leurs sorties réseau, `CharacterSearch`, `VirtualKeyboard`, `scripts/`, `store-analytics/`, contenu de `msix/Assets`.
- **Non instruit faute de source dans le périmètre** : le contenu réel de `defi-corpus.json` et `lessons.json` (droits des extraits), et le fichier de réglages lu par `LearningModule.cs:229` (`LearningTweaks`), qui écrit une ligne dans `error.log` à chaque chargement.
