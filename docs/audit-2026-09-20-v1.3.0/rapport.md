# Audit complet — AZERTY Global v1.3.0 (candidat `4d5da24`)

- **Date** : 2026-09-20 · session `2026-09-20-audit-app-azerty-global-v1-3-0` · modèle de synthèse : Claude Fable 5.1 (effort élevé à la demande d'Antoine) · six agents Opus en lecture seule, deux agents de vérification mécanique des citations, un agent `second-avis` adversarial sur le verdict.
- **Objet** : la branche `release/1.2.0-notation-store` à `4d5da24` (29 commits depuis `8775489`, non poussée, 32 commits derrière `origin/main` par décision). Le Store sert la 1.1.0 depuis le 23/07 ; ni la 1.2.0 ni la 1.3.0 n'ont été soumises.
- **Méthode** : lecture du code, des tests, des scripts, du manifeste, des docs et des deux audits précédents ; exécution des trois suites .NET (Debug et Release), du build Release, des contrôles statiques Python ; contrôle direct par la session des trois constats qui fondent le verdict (horodatages du bundle, `check-doc-versions.py`, suite Python et ordre de la CI). Aucune modification du dépôt.
- **Hors périmètre, non vérifié** : la sortie d'ILC (`dotnet publish` interdit à l'audit), la recette VM (non rejouée), le WACK sur la 1.3.0.0, le rendu réel des fenêtres (lecture du code GDI seulement), la page `azerty.global/soutien`.
- **Convention des citations** : `fichier:ligne` relatif à la racine du dépôt (`src/` sous-entendu pour les `.cs`). ✔ = vérifié par la session elle-même ; ✔a = citation retrouvée à la ligne dite par l'agent de vérification ; ↕ = texte présent mais à une autre ligne (ligne corrigée) ; ⏳ = non vérifié.

## 1. Verdict

**Non soumissible en l'état.** Trois bloquants sont dans la chaîne de livraison et la preuve ; le quatrième est dans le câblage du hook : le correctif de l'écart 8 que la 1.3.0 annonce est incomplet et rouvre l'écart dans l'autre sens. La politique de confidentialité, d'abord classée bloquante, est déclassée en majeur par le second avis (§8).

| # | Bloquant | Preuve | Ce qui lève le bloquant |
|---|---|---|---|
| B1 | **Le bundle 1.3.0.0 est antérieur aux trois derniers commits de code.** Huit fichiers de `src/` sont plus récents que les binaires publiés (x64 17:26:17, arm64 17:26:21 ; bundle 17:27:22) : `ConfigManager.cs` (19:02), `TrayApplication.cs`, `ProductIdentity.cs`, `TrainingReminders.cs`, `lessons.json` (ressource embarquée) et trois fichiers de test (18:57 à 19:02), soit le contenu de `d356841`, `fb2ae86`, `06208ed`. Le bundle ne contient donc ni le rattrapage d'avis ni les quatre ports. `404d68a` (commité 17:30) **est** dans le binaire : ses fichiers datent de 17:20. Publish ↔ bundle est sain (SHA-256 identiques après extraction des deux `.msix`, mesuré par le second avis). ✔ | `find src -newer publish/AZERTY Global.exe` ✔ ; `Pack-MSIX.ps1:84-90` ne teste que l'existence du publish ✔ ; `Verify-Release.ps1:192-206` compare publish ↔ bundle, jamais source ↔ publish ✔ | Rejouer `dotnet publish` x64 et arm64, `Pack-MSIX.ps1`, `Verify-Release.ps1`, WACK, puis archiver le rapport WACK 1.3.0.0. Ajouter à `Pack-MSIX.ps1` un refus si un fichier de `src/` est plus récent que le publish (piège déjà consigné : `script-de-packaging-livre-un-binaire-perime`). |
| B2 | **La CI est rouge sur `release/**` avant même de compiler.** `check-doc-versions.py` sort en 1 avec 9 erreurs ; le test `test_aucune_erreur_dans_les_documents_de_parc` (`scripts/tests/test_check_doc_versions.py:223`) échoue donc ; `ci.yml:60` lance cette suite **avant** `Setup .NET 8 SDK` (`:62`), les publish, les trois `dotnet test`, le pack, `Verify-Release` et l'attestation. Sur cette branche, aucun artefact CI ni attestation SLSA ne peut sortir. Nuance du second avis : c'est un bloquant **de porte**, pas de soumission — le bundle soumis est construit localement ; et le correctif ne solde que les 9 erreurs, les 32 « attentes » du kit DSI (« document non publiable en l'état ») restent (AG130-51). ✔ | Sortie verbatim de `check-doc-versions.py` (9 erreurs, 32 attentes) et de `unittest discover` (`Ran 116 tests … FAILED (failures=1)`) ✔ | Passer `version-app: 1.3.0` dans `Distribution Entreprises.md` (bloc + L18), `entreprise/Note RGPD - Établissements.md` (bloc) et les 5 documents de `sources/legacy/…` (Distribution Entreprises, Note informatique, LISEZMOI-DSI, LISEZ-MOI, SIGNATURE), ou déclarer 1.2.0 historique. Relancer jusqu'au 0 erreur. |
| B3 | **La recette VM 1.3.0 n'est pas terminée.** Écart 5 (« Forcer la compatibilité jeu » perd des caractères) ouvert et déclaré bloquant par la recette du 19/09 ; écarts 4, 7, 8 corrigés par `404d68a` **jamais rejoués** ; RET-01 à RET-09 non testés ; 8 scénarios sur 23 non testés ; seul rapport WACK archivé : `APP_VERSION="1.2.0.0"`. | `docs/audit-2026-09-15-v1.2.0/recette-vm-resultats.md`, `recette-vm-v1.3.0-correctifs.md` (agent 00) ; `Archives/wack/2026-09/` (agent 04) | Rejouer la recette sur le bundle reconstruit (B1) : VM-06 à VM-18, RET-01 à 09, écarts 4/7/8, et le geste ajouté par cet audit pour l'écart 8 (§2, AG130-06). Trancher l'écart 5 avec les deux pistes du §2 (AG130-08). Ne se lève pas par un simple rejeu : B4 se corrige d'abord, sinon la recette rejoue un écart 8 rouvert. |
| B4 | **Le correctif de l'écart 8 (`404d68a`) est incomplet : les raccourcis globaux restent armés sous anti-cheat, et l'écart d'origine rouvre dans l'autre sens.** `OnForegroundChanged` n'a que deux branches : `mode == DisabledAntiCheat && !_suspendedForCompatibility` (l.2239) et `mode != DisabledAntiCheat && _suspendedForCompatibility` (l.2275). Une transition directe entre deux applications qui suspendent toutes les deux (`mode` constant, seul le motif change : application « désactivée par l'utilisateur » ↔ jeu anti-cheat ou client d'accès distant) ne tire aucune branche, `ApplyHookState()` (l.490-515) n'est pas rappelé, `ShortcutsWhilePassThrough` garde sa valeur précédente. Sens 1 : Ctrl+Maj+W ouvre une fenêtre sous anti-cheat. Sens 2 : le geste que le commentaire du code décrit par « le geste réel détruisait le travail en cours » (l.502) revient. Confirmé par lecture directe de la session et par le second avis ; **non exécuté**. ✔ | `TrayApplication.cs:2239, 2275, 490-515, 502` ✔ | Mémoriser le dernier `CurrentSuspendReason` et rappeler `ApplyHookState()` quand il change en restant suspendu. Geste VM à ajouter à `recette-vm.md` : application forcée « off » → Alt+Tab direct vers un processus anti-cheat → Ctrl+Maj+W ne doit rien ouvrir ; et l'inverse. Aucun test possible aujourd'hui (`TrayApplication` non testé) : un test de `ShortcutsWhilePassThrough` sur les trois transitions est le témoin à écrire. |

Ce qui est **sain et prouvé** aujourd'hui : 501 tests verts en Debug comme en Release (18 / 157 / 326), 0 ignoré ✔ (agent 05) ; build Release sans avertissement de trimming ni d'AOT côté analyseurs ; aucun paquet vulnérable sur les trois projets livrés ; **aucun appel réseau sortant** dans `src/` (0 `HttpClient`, `WebRequest`, `Socket`, `Process.Start`) — seules 12 ouvertures d'URL constantes par `ShellExecuteW` et l'API d'avis du Store ; `usage-stats.json` sans caractère, séquence ni horodatage par frappe ; noms de processus hachés (HMAC) dans `error.log` ; manifeste cohérent (CLSID des toasts identique au code et tenu par un test, 4 assets présents aux bonnes tailles, arm64 réellement ARM64, `runFullTrust` seule capacité) ; politiques d'entreprise identiques nom pour nom code / ADMX / `.reg`, `.reg` en UTF-16 LE avec BOM et CRLF.

## 2. Findings

Échelle : **Bloquant** (empêche la soumission), **Majeur** (à corriger avant la soumission ou explicitement reporté en 1.3.1 par Antoine), **Mineur** (1.3.1 ou plus tard), **Observation** (à savoir, rien à faire ou hors dépôt).

### 2.1 Bloquants

| Id | Gravité | Constat | Preuve | Correction minimale | Test / témoin |
|---|---|---|---|---|---|
| AG130-01 | Bloquant | Bundle 1.3.0.0 périmé de trois commits (B1) | horodatages ✔ ; `Pack-MSIX.ps1:84-90` ✔ ; `Verify-Release.ps1:192-206` ✔ | rebuild complet + garde de fraîcheur dans `Pack-MSIX.ps1` | aucun ; à ajouter dans `scripts/tests/` : un `.cs` plus récent que le publish fait échouer le pack |
| AG130-02 | Bloquant | CI rouge sur `release/**` : `check-doc-versions.py` exit 1 (9 erreurs) → `test_aucune_erreur_dans_les_documents_de_parc` échoue → job `build` bloqué avant tout `dotnet` (B2) | sorties verbatim ✔ ; `ci.yml:60` < `:62` ✔ | `version-app: 1.3.0` dans les 7 documents ou statut historique | le test existe et fait son travail ; c'est lui qui bloque |
| AG130-03 | Bloquant | Recette VM 1.3.0 incomplète, écart 5 ouvert, `404d68a` non rejoué, aucun WACK 1.3.0.0 (B3) | agent 00 §B ; agent 04 §9 | rejouer sur le bundle reconstruit | `recette-vm.md` : ajouter le geste AG130-06 |
| AG130-06 | **Bloquant (B4)** | Trou de transition directe du correctif de l'écart 8, dans les deux sens : voir B4. | `TrayApplication.cs:2239, 2275, 490-515, 502` ✔ | mémoriser le motif de suspension, rappeler `ApplyHookState()` | aucun ; test de `ShortcutsWhilePassThrough` à écrire |

### 2.2 Majeurs

| Id | Gravité | Constat | Preuve | Correction minimale | Test / témoin |
|---|---|---|---|---|---|
| AG130-04 | Majeur (hors dépôt) | Politique de confidentialité datée 2026-07-11, antérieure aux fonctions 1.2/1.3 — Store 10.5.1. Déclassée par le second avis : la certification vérifie que l'URL répond, pas son contenu ; c'est une obligation de conformité **avant publication**, pas un refus de certification attendu. | fetch ✔ | mettre à jour § 4.2 du site | aucun |
| AG130-05 | Majeur | **Le Changelog 1.3.0 et la Fiche Store ne mentionnent ni `404d68a`** (Alt+Tab, Ctrl+Maj+W, fenêtre Paramètres : 8 fichiers, +227 l. dans `SettingsWindow.cs`, 158 l. dans `KeyboardHook.cs`) **ni `17382e0`** ; le Changelog titre « 19 septembre 2026 » alors que trois commits de code sont du 20. Store 10.1.4 demande des métadonnées exactes. | recherche de « Alt+Tab », « Ctrl+Maj+W » et « défil » dans `Changelog.md` : 0 résultat ✔ ; `git log` ✔ ; agent 04 §3 | ajouter les trois correctifs à « Version 1.3.0 », dater au 20/09, reporter dans `Fiche Store.md` | `check-doc-versions.py` ne lit pas le Changelog ; aucun témoin |
| AG130-07 | Majeur | **`LLKHF_INJECTED` n'est jamais lu** (seul `LLKHF_EXTENDED` existe). Les frappes injectées par AutoHotkey, le clavier visuel Windows ou un outil d'accessibilité sont remappées comme des frappes physiques (le Bureau à distance, lui, est déjà couvert par `GameRegistry.RemoteAccessProcesses` : mstsc, msrdc, AnyDesk, TeamViewer, Parsec, rustdesk — second avis) ; l'auto-reconnaissance repose sur le seul marqueur `dwExtraInfo` tiré au démarrage. | `KeyMapper.cs:26` ✔a (agent 01 §2) | lire `flags & LLKHF_INJECTED` et laisser passer ce qui n'est pas marqué par l'application (choix produit : remapper ou non le clavier visuel) | aucun ; ajouter un test `KeyMapper` avec `flags = LLKHF_INJECTED` |
| AG130-08 | Majeur | **Écart 5 : deux mécanismes candidats, non tranchés.** (a) AltGr est détecté comme RAlt+LCtrl mais émis comme `VK_RMENU` seul, `wScan = 0`, sans bit étendu — c'est le chemin du `@`. (b) Si l'instantané de premier plan devient périmé entre la touche morte (consommée) et la lettre, la lettre sort nue et la touche morte disparaît — ce qui reproduit « `^a` rend `a` » et « même geste, deux résultats ». Les deux tests existants figent la forme actuelle de (a), ils ne la valident pas. | `KeyMapper.cs:1181` ✔a, `KeyMapper.cs:489-492` ✔a (agent 01 §4, §8) | reproduire en VM avec journal de compatibilité activé ; pour (a), émettre `VK_RMENU` avec `KEYEVENTF_EXTENDEDKEY` et le scan code ; pour (b), conserver la touche morte pendante quand le contexte d'émission est indisponible | tests figeant la forme : à réécrire en tests de comportement |
| AG130-09 | Majeur | **Perte silencieuse à l'émission.** `SendInput` est déclaré sans `SetLastError` et la compensation ne se déclenche que sur un lot **partiel** : un retour `0` (UIPI, bureau sécurisé, session verrouillée) perd le caractère sans trace, alors que la touche physique a déjà été bloquée. | `Win32.cs:64` ✔a, `KeyMapper.cs:1012` ✔a (agent 01 §5) | traiter `sent == 0` comme échec : journaliser (compteur) et, à défaut d'émission, ne pas bloquer la touche | aucun ; `MockWin32Api` peut rendre 0 |
| AG130-10 | Majeur | **Hook perdu : aucune détection.** Windows retire silencieusement un hook bas niveau dont le rappel dépasse `LowLevelHooksTimeout` ; l'application réinstalle à l'aveugle toutes les 60 s, sans horodater le dernier rappel reçu. Entre-temps, l'utilisateur tape en AZERTY natif sans signal. | `TrayApplication.cs:458` ✔a (agent 01 §1) | horodater le dernier rappel ; si une frappe est vue par `GetAsyncKeyState`/`WM_INPUT` sans rappel depuis N s, réinstaller et le dire dans le journal | aucun (`KeyboardHook` jamais instancié par un test) |
| AG130-11 | Majeur | **Configuration : réécriture destructrice et concurrence.** (a) Tout nombre repasse par `double`, et tout objet, tableau ou `null` inconnu est réécrit **en chaîne** (`{"x":{"a":1}}` devient `"x":"{\"a\":1}"`) : les clés inconnues survivent, pas leur forme. (b) Le mutex est **par session** (`Local\AZERTYGlobalSingleInstance.<SID>`) : RDP + console du même compte lancent deux instances sur les mêmes `config.json`, `usage-stats.json`, `error.log` ; le `.tmp` de sauvegarde est un chemin fixe, l'`IOException` de collision est avalée, dernier écrivain gagnant. | `ConfigManager.cs:991,993` ✔a, `Program.cs:30` ✔a (agent 02 §1-2) | (a) conserver le `JsonElement` brut des clés inconnues ; (b) `Global\` + SID ou verrou de fichier, `.tmp` suffixé par le PID | aucun (mutex, atomicité, écritures concurrentes : « aucun trouvé ») |
| AG130-12 | Majeur | **Documents publics faux.** `README.md:17` : « version 1.2.0, manifeste MSIX local en 1.2.0.0 […] Aucun package n'a encore été produit ni soumis » — trois assertions fausses. `Fiche Store.md:159` : `99% OF YOUR HABITS PRESERVED`, formule interdite par le brief du dépôt (le claim est « 99 % des frappes ») ; survit aussi dans `README.md:9` et `screenshot-5-changements.html:273`. | agent 04 §2, §9 ✔a | corriger les trois ; le contrôle `check-doc-versions.py` ne lit pas `README.md` : l'ajouter à sa liste | `check-doc-versions.py` : périmètre à étendre |
| AG130-13 | Majeur | **Couverture : 21 fichiers de production sur 58 ne sont nommés par aucun test, soit 9 572 lignes sur 27 653 (34,6 %) — borne basse de la non-couverture : nommer un type n'est pas l'éprouver, la part réellement exercée est plus faible encore.** Hors GDI : `StoreReview.cs` (99 l., `TryShow`, cœur de la 1.3.0) et `SecureInputDetector.cs` (218 l.) sont à zéro. `SettingsWindow.cs` (2 013 l.) : zéro test, et `404d68a` y ajoute 227 lignes sans une ligne de test. `ShortcutsWhilePassThrough` : aucun test. Câblage du chien de garde 250 ms (`TrayApplication.cs:540, 826-832`) : aucun test. | agent 05 §2, §6 ✔a (mesure par script) | au minimum : test de `ShortcutsWhilePassThrough` sur les trois transitions ; test de `StoreReview.TryShow` sur un `IWin32Api` simulé | à écrire |
| AG130-14 | Majeur | **Témoins de mutation : trois défauts.** (a) `run_suite()` des lots B, C, D ne lance que `AZERTYGlobal.Tests` : 175 tests (35 %) invisibles à toute mutation. (b) `witness-lot-d.py` mutation 3 a une ancre morte (`CollectionEnabled => !AppChannel.CurrentIsSober`, réécrite par le lot C) : le script imprime `ECHEC`, `continue`, `return 0`. (c) `witness-baseline.py` irrejouable (binaire 1.1.0 désinstallé) ; `witness-embedded-resources.py` écrit sous `src/` et lance `dotnet`, ce n'est pas un contrôle statique. Aucun témoin de mutation versionné pour la 1.3.0. | agent 05 §4 ✔a (trois citations de forme seulement : accents restitués, sortie formatée au lieu du gabarit) | (a) lancer les trois suites ; (b) faire sortir en 1 sur ancre absente ; (c) le dire dans `docs/audit-v1.2.0/README.md` | les témoins sont eux-mêmes le sujet |

### 2.3 Mineurs

| Id | Gravité | Constat | Preuve | Correction minimale |
|---|---|---|---|---|
| AG130-15 | Mineur | Effet de bord non documenté : la stratégie `UsageStatsEnabled = 0` sur le canal Store empêche `ActiveDaysCount` de persister, donc les seuils 3 et 10 jours ne sont jamais atteints : **plus aucune sollicitation d'avis**, partage inclus. | agent 02 §4 ✔a | le documenter dans l'ADML et la Note RGPD, ou compter les jours hors des stats |
| AG130-16 | Mineur | Migration `d356841` : `reviewPromptDone` n'est plus lu du tout (une installation 1.1 repart avec deux essais et, `reviewPromptLastShown` étant absente, saute une fois le plancher de 7 jours) ; le drapeau continue d'être **écrit**. Conforme à la décision d'Antoine, mais le drapeau mort est à retirer. | agent 02 §4 ✔a ; `ReviewPromptCount => (int)GetUInt("reviewPromptCount")` ✔ | cesser d'écrire le drapeau ; noter le plancher sauté dans le Changelog |
| AG130-17 | Mineur | Touche morte non annulée au changement d'application : `^` dans A puis Alt+Tab accentue la première voyelle tapée dans B. | `KeyMapper.cs:135-145` ✔a (agent 01 §4) | vider l'état de composition dans `RefreshForegroundContext` |
| AG130-18 | Mineur | Défilement de la fenêtre Paramètres (`404d68a`) : `WM_MOUSEWHEEL` traité sur le parent seul, les deux EDIT de raccourcis et la LISTBOX de compatibilité consomment la molette ; `delta / 120` en entier ignore les pavés tactiles haute résolution (delta 30) ; Tab vers un contrôle hors zone visible ne fait pas défiler (focus invisible). `SB_THUMBTRACK` via `SIF_TRACKPOS`, `WS_VSCROLL` + `SM_CXVSCROLL`, rappel de `FitWindowToContent` sur `WM_DPICHANGED` et changement de langue : corrects. | `SettingsWindow.cs:1057, 436, 444` ✔ (lecture directe) | relayer `WM_MOUSEWHEEL` des enfants au parent ; accumuler le delta ; `EnsureVisible` sur `WM_SETFOCUS` des enfants |
| AG130-19 | Mineur | Debounce mort : `TIMER_FOREGROUND_DEBOUNCE` n'est plus jamais armé (`OnWinEvent` appelle `Recompute()` en direct) ; la branche du wndproc est du code mort ; chaque `EVENT_OBJECT_FOCUS` déclenche un `Recompute` complet (OpenProcess, chemin, heure de démarrage, champ mot de passe UIA, énumération des modules) et `ForegroundChanged` tire dès que `processName != null`. Pré-existant, pas 1.3.0. | `ForegroundMonitor.cs:169-175`, `TrayApplication.cs:834-838` ✔ (lecture directe) | supprimer le timer ou le rebrancher ; mémoriser le dernier `hwnd` pour court-circuiter |
| AG130-20 | Mineur | Notifications : le rappel du Défi a un plancher de 17 h mais **aucun plafond** horaire ; zéro consultation de l'Assistant de concentration, du plein écran ou du mode présentation ; le nom du produit apparaît une fois dans le gabarit, mais Windows ajoute l'attribution sur le canal toast, donc deux fois à l'écran. | agent 02 §10 ✔a | plafond 21 h ; `SHQueryUserNotificationState` avant d'émettre |
| AG130-21 | Mineur | Les ADML et le `.reg` annoncent `%LocalAppData%\AZERTY Global\usage-stats.json`, alors que le chemin réel d'une application empaquetée est `…\Packages\<PFN>\LocalCache\Local\AZERTY Global\` (la Note RGPD porte la bonne forme). | agent 02 §5 ✔a | aligner les trois textes |
| AG130-22 | Mineur | `L.LessonsWindow.cs:23` affiche « Reset stats » en français ; quatre chaînes françaises codées en dur, toutes destinées à `error.log`. | agent 02 §8 ✔a | traduire l'une, laisser les autres |
| AG130-23 | Mineur | `store-analytics.yml:50-57` porte encore l'étape « Conserver une copie de diagnostic » supprimée sur `main` par `1013d2b` (« Keep Store analytics archives in private storage only »). | agent 04 §5 ✔a | reprendre `1013d2b` sur la branche |
| AG130-24 | Mineur | Pas de `global.json` ; `ci.yml:65` = `8.0.x` ; .NET 8 sort du support le 2026-11-10 : la 1.3.0 sera servie sur un runtime hors support sept semaines après une soumission fin septembre. | agent 04 §6 ✔a | épingler `8.0.425` maintenant ; planifier .NET 10 LTS pour la 1.4 |
| AG130-25 | Mineur | `ToastActivation.Activate()` reçoit `appUserModelId` et ne le vérifie jamais, avec `CLSCTX_LOCAL_SERVER` et `REGCLS_MULTIPLEUSE`. Les trois actions sont constantes, aucune URL ni exécutable ne peut sortir d'un argument. | agent 02 §7 ✔a | comparer à `ProductIdentity.AppUserModelId` et ignorer sinon |
| AG130-26 | Mineur | Tests qui ne prouvent rien : `Placeholder.cs:8` (corps vide), `LessonCoreTests.cs:472-479` (deux `Assert.Contains` sur le texte d'un `.ps1` jamais exécuté, déjà signalé le 18/08), `FrenchTypographyTests.cs:53-54` (395 chaînes énumérées avec `catch {}` muet et aucun contrôle de non-vacuité ; la suite Python du même dépôt, elle, le garde). | agent 05 §3 ✔a | supprimer le premier ; exécuter le `.ps1` ; asserter `count > 0` |
| AG130-27 | Mineur | Build Release : 2 × `CS8604` dans `TrayApplication.cs:1394` et `:1407` (colonne 88). | agent 05 §1 ✔a | annoter ou garder la nullité |
| AG130-28 | Mineur | `Sync-LayoutResources.ps1:27-30, 147` copie un fichier sur lui-même, en silence grâce au court-circuit « INCHANGE » `:60-63` (bug reconnu). | agent 04 §4 ✔a | corriger la source |
| AG130-29 | Mineur | Cahier des charges §7 : 5 exigences sur 19 sans preuve dans le dépôt ; les 5 cases « Couches maintenables » (`Cahier des charges.md:284-289`) restent `- [ ]` ; VM-11 contredit l'exigence l.288 (la fenêtre de recherche se ferme à la perte de focus). | agent 00 §C ✔a, agent 04 §9 ✔a | cocher ce qui est prouvé, amender l.288 ou corriger la fenêtre |
| AG130-30 | Mineur | CI : toutes les actions épinglées par tag mobile (`@v4`), aucune par SHA ; aucun job `maquettes` (le brief en cite un). `permissions:` déclaré, ARM64 construit, 3 suites Release une par une, `provenance` non bloquant par conception, `continue-on-error` seulement sur BinSkim. | agent 04 §5 ✔a | épingler par SHA ; retirer la mention du job `maquettes` des docs |

### 2.4 Observations

| Id | Constat | Preuve |
|---|---|---|
| AG130-31 | **Champ mot de passe** : le remappage et les touches mortes **continuent** d'émettre dans un champ sécurisé (comportement voulu et testé) ; seules la recherche et les couches s'éteignent. À dire tel quel dans la fiche Store et la politique de confidentialité. La détection UIA rend la **dernière valeur connue globale** après 30 ms, sur le thread qui porte le hook : faux positifs et faux négatifs structurels. | `SecureInputDetector.cs:85` ✔a (agent 01 §7) |
| AG130-32 | **Fail-closed du premier plan ne bloque pas la frappe** : pendant l'instantané périmé (≤ 250 ms ou jusqu'au prochain événement), les touches passent **non remappées**, l'utilisateur voit de faux caractères, pas une absence. Le chien de garde lui-même est sain : une lecture `GetForegroundWindow` par tick, `Recompute` seulement si périmé, pas de bulle sur premier plan nul. | `ForegroundMonitor.cs:100-106, 116-123`, `TrayApplication.cs:467-476, 827-833` ✔ |
| AG130-33 | Coût du rappel du hook par touche : 8 `GetAsyncKeyState`, 2 allocations + `ToUnicode` par keydown émetteur, **6 `GetForegroundWindow` par caractère émis** (`IsEmissionSuspended` réévalué six fois). Sous `LowLevelHooksTimeout` par défaut (300 ms), mais chaque appel Win32 est un aller-retour noyau sur le chemin critique. | `KeyMapper.cs:306-307` ✔a (agent 01 §1) |
| AG130-34 | **Store 10.8.2 — dons.** Le menu « Soutenir le projet » (canal Store seulement) ouvre `https://azerty.global/soutien` dans le navigateur. La politique : « You must use the Microsoft payment request API or a secure third-party purchase API to receive voluntary donations from users » et « You must also state clearly that Microsoft is not the fundraiser ». L'application ne reçoit rien elle-même ; un lien sortant vers HelloAsso est la pratique courante. La phrase « Microsoft n'est pas le collecteur » n'apparaît ni dans la fiche Store ni dans le menu. | `TrayApplication.cs:692, 1289` ✔, `L.Tray.cs:170` ✔ ; politique v7.20 ✔ |
| AG130-35 | Store 10.2.8 (consentement pour modifier l'expérience Windows) : la fiche Store l'invoque pour l'écran de consentement du premier lancement — cohérent avec le code lu. | `Fiche Store.md:317` ✔ (lecture directe) |
| AG130-36 | Manifeste : `LargeTile.png` (310×310) présent mais non référencé ; pas de `targetsize-*` ; pas de `resources.pri` malgré deux `<Resource Language>` — non requis pour une application Win32 sans ressources MRT, à confirmer au WACK. Bundle non signé (attendu, le Store signe). | agent 04 §1 ✔a |
| AG130-37 | Le seul élément en clair qui nomme des programmes de l'utilisateur est la liste des processus pour lesquels il a posé un override, dans `config.json` ; tout le reste est haché. Les partages sont du texte brut, jamais d'image. | agent 02 §9 ✔a |
| AG130-38 | `list-identity-literals.py` exclut `TypingEngine.Core` et `TypingEngine.Windows` (`SKIP_DIRS`), du code de production ; l'angle mort est occupé par trois occurrences de son propre motif dans `ToastActivationTests.cs:26, 69`. Déjà noté en août, toujours là. | agent 05 §5 ✔a |
| AG130-39 | Écarts de l'audit du 15/09 : les 11 findings AG120-01 à 11 sont déclarés corrigés (`corrections.md:11-21`) ; trois constats non numérotés restent ouverts : `gen-appinstaller` pour le canal AMCF (`rapport.md:257`), CI sans exécution ARM64 (`rapport.md:249` — l'ARM64 est **construit**, jamais **exécuté**), sollicitation d'avis en session longue reportée. | agent 00 §A ✔a |
| AG130-50 | **Captures d'écran du Store non contrôlées** contre l'interface 1.3.0 (menu passé de 18 à 12 lignes, nouvelles entrées). Le second avis relève des captures datées de mars à juin 2026 dans la fiche ; Store 10.1.1 exige qu'elles représentent l'application soumise. À contrôler avant la soumission. | `msix/Fiche Store.md` (liste des captures) ⏳ ; signalé par le second avis |
| AG130-51 | Les 32 « attentes » de `check-doc-versions.py` (kit DSI : noms de bundle 1.1.0, empreintes « en-attente », « document non publiable en l'état ») ne sont pas levées par B2 ; elles se lèvent par `update_kit_version.py` et les empreintes du bundle reconstruit. Canal AMCF, pas Store. | sortie verbatim ✔ |
| AG130-52 | La montée de version depuis le parc 1.1.0 (migration de `config.json`, `usage-stats.json`, `lessons-progress.json`) n'a été lue qu'à travers `reviewPromptDone` (AG130-16) ; le reste de la migration dans `ConfigManager` n'a pas été audité. | limite, signalée par le second avis |

### 2.5 Fenêtres, rendu, accessibilité

Prémisse corrigée en cours d'audit : **il n'existe aucun système de thème dans cette branche** (0 occurrence de `Theme`, `GetSysColor`, `SPI_GETHIGHCONTRAST`, `WM_SETTINGCHANGE`) ; les couleurs sont des constantes `CLR_*` en dur. Le rendu réel n'a pas été observé : tout ce qui suit est lu dans le code.

| Id | Gravité | Constat | Preuve | Correction minimale | Test / témoin |
|---|---|---|---|---|---|
| AG130-40 | Majeur | **Navigation clavier : la boucle de messages n'appelle ni `IsDialogMessageW` ni `TranslateAcceleratorW`** (GetMessage → TranslateMessage → DispatchMessage nus). Les `WS_TABSTOP` sont inertes partout où la fenêtre ne gère pas `VK_TAB` à la main (SettingsWindow le fait via `WM_GETDLGCODE` ; les autres, non) ; `BS_DEFPUSHBUTTON` n'est pas activé par Entrée ; même omission dans la boucle modale de `PauseDurationDialog`. Nuance du second avis : `OnboardingWindow` route Entrée et les flèches, et un BUTTON natif focalisé répond ; c'est la circulation par Tab entre contrôles qui manque, pas toute navigation clavier. | `TrayApplication.cs:544-552` ✔ ; `grep IsDialogMessage src/` : 0 ✔ ; `PauseDurationDialog.cs:66` ✔a | `IsDialogMessageW(hwndActive, ref msg)` avant `TranslateMessage` pour les fenêtres à contrôles, ou `WM_GETDLGCODE` uniforme | aucun (0 test sur le focus, agent 03 §9) |
| AG130-41 | Mineur (déclassé par le second avis : les contrôles Win32 natifs restent accessibles) | **Zéro UIA/MSAA** : aucun `WM_GETOBJECT`, `IAccessible` ni `NotifyWinEvent`. Les zones dessinées (clavier virtuel, leçons, statistiques, boutons owner-draw) n'exposent rien à un lecteur d'écran ; aucun `DrawFocusRect`/`ODS_FOCUS` sur les boutons owner-draw de `LearningModule`. Les contrôles Win32 natifs (boutons, cases, EDIT) restent, eux, accessibles. | comptage de `WM_GETOBJECT`, `IAccessible` et `NotifyWinEvent` dans `src/` : 0 ✔ | à minima un `DrawFocusRect` sur `ODS_FOCUS` ; l'exposition UIA des zones dessinées est un chantier 1.4 | aucun |
| AG130-42 | Majeur | **DPI : `LessonsWindow` ne traite pas `WM_DPICHANGED`** (les 9 autres fenêtres le font) et lit son DPI sur le DC du moniteur principal. Sur 1920×1080 à 175 %, la fenêtre des leçons demande ~1960×1330 px de zone client sans plafond, et l'accueil ~1060 px de haut ; seul `LearningModule` borne à 90 % de la zone de travail. Fontes en dur (`-16`, `-14`, `-15`) dans trois fenêtres. | liste des fichiers traitant `WM_DPICHANGED` ✔ (LessonsWindow absent) ; `LessonsWindow.cs:158-161` ✔a, `LearningModule.cs:819` ✔a | `GetDpiForWindow` + `WM_DPICHANGED` dans LessonsWindow ; plafonner à la zone de travail comme `LearningModule` | aucun (0 test DPI) |
| AG130-43 | Mineur | **Barre de titre sombre forcée sur contenu clair** : `EnableDarkTitleBar` pose `useDarkMode = 1` sans lire le réglage système (`AppsUseLightTheme`) ; 10 fenêtres l'appellent, 7 définissent un fond `0x00DDDDDD` (About, Recherche, Conflit de disposition, Couches maintenables, Accueil, Paramètres, Statistiques). | `Win32.cs:493-499` ✔ ; compte des appelants et des fonds ✔ | lire `AppsUseLightTheme` (HKCU `…\Themes\Personalize`) et suivre le système | aucun |
| AG130-44 | Mineur | **Fuites GDI sur exception** : `SettingsWindow` et `OnboardingWindow` peignent sans `try/finally` ; `LearningModule` met `EndPaint` hors du `finally` ; `VirtualKeyboard` crée 7 objets dont l'appelant avale l'exception → fuite silencieuse à chaque `WM_PAINT` fautif. Quatre `Dispose()` sans garde ni remise à zéro des handles. Le chemin nominal est propre (les 5 doubles tampons restaurent `hBmpOld` avant `DeleteObject`). | `SettingsWindow.cs:1709`, `OnboardingWindow.cs:1069`, `LearningModule.cs:1933`, `VirtualKeyboard.cs:1054-1065, 1014-1018`, `SettingsWindow.cs:1994` ✔a | `try/finally` autour de chaque `BeginPaint`/`SelectObject` | 1 seul test GDI (`Audit120BitmapTests`, arithmétique du masque) |
| AG130-45 | Mineur | **Fenêtre Paramètres : pas de défilement clavier** (seul Échap est traité) ni d'amenée à l'écran du contrôle focalisé ; le plancher `Math.Max(S(200), workH - chromeH)` prime sur la zone de travail. Complète AG130-18. | `SettingsWindow.cs:1243` ✔a, `:602` ↕ (le rapport d'agent disait 601) | `VK_UP/DOWN/PRIOR/NEXT` → `WM_VSCROLL` ; `EnsureVisible` sur `WM_SETFOCUS` | aucun |
| AG130-46 | Mineur | **Clavier virtuel : 6 touches mortes dessinées en marque combinante nue** (U+0323, U+030F, U+031B, U+0309, U+0311, U+0326). `GetDeadKeySymbol` rend le caractère brut (`dk_double_grave => "̏"`) et `VirtualKeyboard` n'applique jamais le préfixe `◌` que `KeyboardRenderer` applique (0 contre 3 occurrences). La liste des 7 touches à cercle pointillé est en dur dans `KeyboardRenderer` et dupliquée dans `LearningModule`. | `TrayApplication.cs:1878-1890` ✔ ; `VirtualKeyboard.cs:1157-1159` ✔ ; `KeyboardRenderer.cs:83-91`, `LearningModule.cs:2893` ✔a | préfixer `◌` (U+25CC) quand `char.GetUnicodeCategory` est `NonSpacingMark` ; une seule liste | aucun |
| AG130-47 | Mineur | **Les 29 noms de touches mortes sont en français seulement** (`VirtualKeyboard.cs:100-126`) ; le corpus de leçons est dupliqué (`LearningModule.cs:30/36` = `LessonCatalog.cs:206/208`) et porte 3 apostrophes droites et des espaces ordinaires dans « … » et avant `!`, hors du filet de `FrenchTypographyTests` (qui n'énumère que `typeof(L)`) ; aucun U+202F (espace fine insécable) dans les chaînes affichées. | agent 03 §6 ✔a ; nuance du vérificateur : un U+202F existe dans `UsageStats.cs:232`, un `case` d'analyse, pas une chaîne affichée | passer les noms par `T(fr, en)` ; étendre `FrenchTypographyTests` au corpus | `FrenchTypographyTests` : périmètre à étendre |
| AG130-48 | Observation | `WS_CLIPCHILDREN` manque sur 6 fenêtres qui peignent sous leurs enfants (scintillement possible) ; la brosse d'instance sert de brosse de classe dans 8 fenêtres, motif que `LearningModule.cs:783-791` documente comme ayant déjà causé un crash. | agent 03 §5 ✔a | ajouter le style ; brosse de classe dédiée | aucun |
| AG130-49 | Observation | Textes : le claim est bien « 99 % de vos frappes préservées » dans l'application (la formule interdite ne survit que dans la fiche Store et le README, AG130-12) ; « quelques heures » nulle part ; « impossible » jamais sur les majuscules accentuées ; nom du produit une seule fois par gabarit de notification. | agent 03 §6 ✔a | — | `FrenchTypographyTests` |

## 3. Recette VM — état au 20/09

| Bloc | État | Source |
|---|---|---|
| VM-01 à VM-23 | 9 VERT · 6 PARTIEL · 8 NON TESTÉ | `recette-vm-resultats.md` (agent 00 §B) |
| RET-01 à RET-09 | non testés | idem |
| Écart 4 (Paramètres), 7 (Ctrl+Maj+W), 8 (Alt+Tab) | corrigés par `404d68a`, **jamais rejoués** ; le correctif de l'écart 8 est incomplet (AG130-06) | `recette-vm-v1.3.0-correctifs.md` |
| Écart 5 (Forcer compat jeu perd des caractères) | **ouvert, déclaré bloquant** ; deux pistes (AG130-08) | idem |
| Écart 6 | non reproduit, non clos | idem |
| WACK | un seul rapport archivé, `APP_VERSION="1.2.0.0"` | `Archives/wack/2026-09/` |

Le bundle sur lequel tout cela sera rejoué doit être celui reconstruit après B1 : la recette du 19/09 portait déjà un binaire qui n'est plus celui de la branche.

## 4. Conformité Microsoft Store (politiques v7.20, ms.date 2026-09-14)

| Clause | Objet | État |
|---|---|---|
| 10.1.4 | métadonnées distinctes, informatives, exactes | ⚠️ Changelog et fiche muets sur `404d68a` (AG130-05) ; `README.md:17` faux (AG130-12) ; captures non contrôlées (AG130-50) |
| 10.2.8 | consentement pour modifier l'expérience Windows | ✅ écran de consentement au premier lancement (AG130-35) |
| 10.5.1 | politique de confidentialité obligatoire et à jour pour les produits Win32 | ⚠️ Majeur, avant publication : datée 2026-07-11, antérieure aux fonctions 1.2/1.3 (AG130-04) |
| 10.5.x | collecte de données | ✅ aucun réseau sortant, aucune frappe persistée (§1) ; à décrire dans la politique |
| 10.8.2 | dons volontaires | ⚠️ lien sortant vers `/soutien` sur le canal Store ; ajouter « Microsoft n'est pas le collecteur » (AG130-34) |

## 5. Changelog 1.3.0 contre les commits du 20/09

| Commit | Heure | Objet | Ligne dans « Version 1.3.0 » |
|---|---|---|---|
| `2a10fe3` | 17:20 | store-analytics : republier les totaux agrégés | hors app, n/a |
| `404d68a` | 17:30 | Alt+Tab, Ctrl+Maj+W, fenêtre Paramètres | **aucune** ✔ |
| `4451bf5` | 17:40 | fetch_totals : totaux complets | hors app, n/a |
| `d356841` | 18:58 | `reviewPromptDone` ne consomme plus d'essai | présente (« rattrapage d'avis ») |
| `fb2ae86` | 18:59 | fins de ligne de `ConfigManager.cs` | technique, n/a |
| `06208ed` | 19:00 | quatre ports isolés de `main` | présente (« quatre ports ») |
| `4d5da24` | 19:01 | changelog | — |

Le Changelog est titré « 19 septembre 2026 » (`Changelog.md:3`) ✔ ; quatre commits de code sont du 20. Le message de `404d68a` annonce « 495 » tests, la branche en compte 501 ✔a.

## 6. Dettes de preuve

Ce que le dépôt affirme sans témoin, classé par ce qu'il faudrait pour le prouver :

1. **Un binaire livré correspond à la source** : rien ne le mesure (AG130-01). Un contrôle `LastWriteTime(src/**) < LastWriteTime(publish)` dans `Pack-MSIX.ps1` suffit.
2. **Les raccourcis sont désarmés sous anti-cheat** : aucun test, trois sites de production (AG130-06, AG130-13).
3. **La fenêtre Paramètres défile et se redimensionne** : 2 013 lignes, zéro test (AG130-13, AG130-18).
4. **La sollicitation d'avis tire au bon moment** : `MaybeShowReviewPrompt` privée, non testée ; `StoreReview.TryShow` à zéro (AG130-13).
5. **Les témoins de mutation prouvent ce qu'ils disent** : 35 % des tests hors de leur vue, une ancre morte tolérée (AG130-14).
6. **Le hook survit** : `KeyboardHook` jamais instancié par un test (AG130-10).
7. **Les documents disent la vraie version** : le contrôle existe et il est rouge (AG130-02) ; il ne lit pas `README.md` ni `Changelog.md` (AG130-05, AG130-12).
8. **Les fenêtres se traversent au clavier et se lisent à l'écran** : aucun test sur le focus, le DPI, le défilement ou le thème ; un seul test GDI (AG130-40 à 44).

## 7. Ordre de marche proposé (aucune correction appliquée par cet audit)

1. Versions des documents → `check-doc-versions.py` à 0 erreur → CI verte sur la branche (B2). Une heure. Les 32 attentes suivent le bundle (AG130-51).
2. Corriger B4 (trou de transition de l'écart 8) : une dizaine de lignes dans `OnForegroundChanged`, plus le test de `ShortcutsWhilePassThrough`. Sans lui, la recette rejoue un écart rouvert.
3. Changelog et fiche : `404d68a`, `17382e0`, B4, date du 20/09 (AG130-05). Trente minutes.
4. Décider AG130-07 (`LLKHF_INJECTED`) et AG130-40 (`IsDialogMessageW`) : dans la 1.3.0 ou reportés. Petits et localisés ; le second demande une recette de la fenêtre Paramètres, qui gère déjà Tab à la main. Les autres majeurs (AG130-08 à 11, 42) demandent une reproduction ou une décision produit et vont en 1.3.1.
5. Rebuild x64 + arm64, pack, `Verify-Release.ps1`, WACK archivé (B1).
6. Recette VM sur ce bundle : VM-06 à 18, RET-01 à 09, écarts 4/5/7/8, gestes B4 dans les deux sens (B3).
7. Politique de confidentialité § 4.2 sur le site (AG130-04) ; phrase 10.8.2 sur `/soutien` ou dans la fiche (AG130-34) ; captures du Store contrôlées (AG130-50).
8. Soumission.

## 8. Méthode et limites

- Six rapports d'agents, en annexe dans `agents/` : `00-base-audits-precedents.md`, `01-moteur-de-frappe.md`, `02-coquille-donnees.md`, `03-fenetres-rendu.md`, `04-empaquetage-ci-docs.md`, `05-tests-preuves.md` ; vérification des citations dans `agents/verif/` ; revue adversariale dans `agents/second-avis.md`.
- Vérification mécanique des citations : 01 → 103 citations, 102 exactes, 0 décalée, 0 absente · 03 → 74, 55 exactes, 8 décalées (lignes corrigées ici), 0 absente, 3 comptages sur 20 en désaccord (`WM_DPICHANGED` 7 → 9 fichiers, un U+202F dans un `case`, table NBSP) · 04 → 200, 178 exactes, 0 décalée, 0 absente · 05 → 140, 68 exactes, 0 décalée, 3 absentes de forme seulement (accents, sortie formatée), 69 comptages sans numéro de ligne. · 00 → 206 total · EXACT 206 · DÉCALÉ 0 · ABSENT 0 · NON VÉRIFIABLE 0 · 02 → 133 total · EXACT 130 · DÉCALÉ 0 · ABSENT 0 · NON VÉRIFIABLE 3.
- Second avis adversarial (agent `second-avis`, même famille de modèle que la synthèse) : B1 tient, preuve reformulée par fichiers ; B2 tient avec nuance (porte CI) ; la politique de confidentialité est déclassée de bloquant à majeur ; AG130-06 est promu bloquant et étendu aux deux sens ; AG130-41 déclassé ; AG130-07 et 40 nuancés ; la phrase de couverture est corrigée. Il recommande une passe d'un **autre fournisseur** avant clôture sur B4 (machine à états lue, non exécutée) et AG130-04 (lecture d'une politique Store) — non faite ici, décision d'Antoine (envoi de contenu chez un tiers).
- Les agents rendent des pointeurs ; la gravité, le verdict et l'ordre de marche sont de la session.
- Les constats notés ⏳ n'ont pas été relus à la ligne par la session : leur citation vient de l'agent, la vérification mécanique dit si le texte est bien là où il est dit.
- Non couvert : sortie d'ILC, recette VM, WACK 1.3.0.0, rendu réel des fenêtres, page `/soutien`, captures du Store (AG130-50), migration depuis la 1.1.0 au-delà de `reviewPromptDone` (AG130-52), code de `LearningModule.cs` (3 105 l.) et `VirtualKeyboard.cs` (1 377 l.) au-delà de leur couverture de test.
